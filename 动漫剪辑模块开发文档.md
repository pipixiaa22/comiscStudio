# CommentaryDesk Studio 动漫剪辑模块开发文档

> 日期：2026-09-07  
> 依据：`CommentaryDesk_Development_Document_V1.0.md`、现有漫画模块、Phase 6 辅助窗口及真人录音流程  
> 目标：在现有 Studio 中增加动漫选段能力，与漫画模块共用文案、素材绑定、故事板和交付体系。  
> 文档性质：基于当前代码的增量开发方案；交互参数与性能指标是待验证的实施约定，不代表已有用户实测结论。

## 1. 产品结论：两个工作区，一套创作核心

用户的判断成立：漫画和动漫都在建立“这段解说使用哪些画面”的关系。差别主要在素材选择器：漫画选择整页或空间区域，动漫选择视频中的时间区间。

因此 Studio 对用户提供“漫画工作区”和“动漫工作区”，底层共用一个 Project、一套 NarrationBlock 和有序 MediaAsset 列表。同一 Block 允许图片、PDF 裁切和视频片段混排，例如讲漫画原作时插入一段动画对照。

| 层次 | 漫画 | 动漫 | 共享方式 |
|---|---|---|---|
| 来源浏览 | 图片/PDF 页面列表 | 视频文件/剧集列表 | 共用来源身份、导入与重定位 |
| 画面选择 | 整页、Crop | In/Out 时间区间 | 各自选择器，统一绑定命令 |
| 文案与旁白 | 手写文案、真人录音 | 手写文案、真人录音 | 复用现有 Block 和 voice 字段 |
| 素材整理 | 图片排序、定位原页 | 片段排序、定位原时间 | 共用 assets 顺序与稳定 ID |
| 检查与交付 | 图片预览、导出、拖出 | 片段预览、导出、拖出 | 媒体类型分发，统一任务与清单 |

首页可提供“开始漫画解说 / 开始动漫解说”两个入口，分别打开合适的导入器与初始布局；它们不是互斥项目格式。在项目中切换工作区，不清空 Block、不转换旁白方式、不创建另一个项目。

“剪辑模块”在本产品中指选取和整理素材片段。多轨时间轴、字幕制作、变速、转场、BGM 混音和最终成片仍由剪映完成。

## 2. 与总纲及现有项目的衔接

### 2.1 已有代码可以复用什么

| 当前位置 | 静态检查结果 | 本次改造 |
|---|---|---|
| `src/store/ProjectStoreProvider.jsx`、`projectReducer.js` | 已有 Block、素材、状态、历史和录音命令 | 增加 typed asset 与视频区间命令，不复制 Store |
| `src/shared/domain/projectNormalize.js` | 当前 schemaVersion 为 2，素材缺少统一类型，workspace 仅保留部分字段 | 顺序迁移、类型校验、保留视频工作现场 |
| `electron/services/ProjectService.js` | 已有项目列表、追加来源、重定位；创建/追加只挑选图片/PDF 字段 | 扩展来源白名单，保留 probe 元数据与媒体类型 |
| `src/features/project/projectSources.js` | 按路径关联来源 ID | 扩展视频来源展示，路径不是片段身份 |
| `src/features/assets/` | Crop、素材列表、素材篮和使用索引 | 按媒体类型展示、校验和定位 |
| `src/features/storyboard/model/validateStoryboard.js` | 已有文案/素材缺项和 Crop 检查 | 增加视频边界、流及原文件变更检查 |
| `electron/services/ExportPlanner.js` | 当前只接受图片/PDF，输出固定为 images | 按 type 规划 images/videos，统一编号 |
| `electron/services/ExportWriters.js` | HTML 素材都输出为 img；另有文字估时 SRT | 增加 video 标签，避免把估时当视频精确字幕 |
| `electron/ipc/registerAssistantIpc.js` | 已有快照、准备、token 和原生拖出；准备链路直接调用图片渲染器 | 分离媒体准备服务，接入视频和正确拖动图标 |
| `package.json` | Electron/React/Sharp/PDF 能力已在，未声明 FFmpeg 集成 | 增加可分发的 FFmpeg/ffprobe 工具链 |

现有项目已经有项目中心、来源追加及 Phase 6 代码，不能按总纲第 59 节从公共核心重新开发。本文使用 Anime A1–A5 表示新增里程碑，避免与仓库原有 Phase 1–6 混淆。

### 2.2 明确兼容约定

- 保留当前 `status.edited` 作为“已放画面”，不直接替换为总纲示例的 `placed`；若导入含 placed 的新格式，使用显式映射。
- 当前 Source 的 `kind`、PDF `pdfPath/pageNumber` 保留；新增公共 `mediaType`，PDF 页面仍属于 image 媒体。
- 继续 JavaScript/JSX、reducer 和 Project JSON；总纲的 TypeScript、Zustand、SQLite 是可选架构方向，不是本模块前置条件。
- UI 可逐步使用 CommentaryDesk/解说工作台名称。暂不改已有 userData 路径和 `mangaDesk` 桥接名，避免旧项目或窗口因品牌重命名失联；品牌迁移另作独立任务。
- 真人录音模式继续使用现有音频导出规则，不新增语音识别或自动字幕。文字模式的现有估时 SRT 不自动对齐动漫片段，新视频导出界面必须注明其为估时产物，默认不把它作为精确同步字幕交付。

## 3. 第一版边界

### 必须交付

视频多选导入、元数据读取、可用播放路径、播放/暂停与时间定位、I/O 选段、区间预览、Enter 绑定、区间修改及撤销、素材篮、定位原片段、使用区间标记、混合故事板、精确片段导出、视频原生拖出和旧漫画项目兼容。

缩略图先完成当前视口时间带与稀疏整集总览；不要求导入时立即生成所有精细缩略图。第一版可限制支持格式，但必须清楚显示“可直接预览 / 需生成预览副本 / 暂不支持”，不能仅因扩展名被接受就声称支持播放和导出。

### 后续增强

连续倒放、波形导航、可管理的高清代理、视频空间 Crop、从视频取静帧加入图片资产、镜头标记与批量片段拖出。HDR、交错视频及复杂时间戳素材在未完成正确转换前可明确阻断，不能静默以错误画面导出。

不做自动场景识别、自动选镜头、自动配文案、自动成片、自动剪映工程或强制按解说长度变速。

## 4. 创作者实际工作流

### 4.1 边看边写

1. 进入动漫工作区，选择一集或追加多集视频。
2. 播放理解剧情，随时暂停写当前 Block；不要求先截素材才能写稿。
3. 找到动作开始处按 I，找到结束处按 O，回放选区确认。
4. 按 Enter 将片段加入当前 Block，反馈“已加入 #018，第 2 项，3.400 秒”。
5. 继续选其他片段，或使用现有“完成并新建下一段”。
6. 在 Storyboard 检查，再导出或通过剪映辅助窗口交付。

### 4.2 先写稿再找画面

从 Block 选择一段文案，利用剧集列表、缩略图总览、收藏时间点和已使用区间快速找素材。绑定目标始终显示在播放器旁；切换剧集不改变当前文案段。

### 4.3 先收集候选再决定用途

看见暂时不确定用途的画面时，B 收藏时间点；有完整 In/Out 时可收藏区间。Q 将有效区间加入素材篮。素材篮可以跨剧集收集，之后加入指定 Block；收藏时间点只能用于定位，不能作为零时长视频资产。

这些流程借鉴成熟源素材监视器“先预览、标记 In/Out，再使用选段”的交互习惯。Adobe 官方文档说明了这一流程，但它不证明本产品的效率收益，仍需真实创作验证：[Source Monitor In/Out 工作流](https://helpx.adobe.com/premiere/desktop/get-started/source-and-program-monitor-adjustments/set-in-and-out-points-in-the-source-monitor.html)。

## 5. 工作区布局与操作规则

主体沿用现有结构：左侧为可调宽的素材区，中间是文案/录音，右侧是当前 Block 素材。动漫素材区内部包含剧集选择、播放器、时间码、In/Out 和缩略图时间带。剧集栏可折叠，播放器宽度优先于次要信息；不把视频硬塞进漫画缩略图卡片。

播放器持续显示：来源文件、播放位置、In、Out、区间时长、当前绑定 Block、预览原声状态、素材交付是否保留原声。时间带用不同样式区分当前选区、已使用区间与收藏点，颜色之外增加标签或边界。

| 按键 | 动漫播放器聚焦时的行为 |
|---|---|
| Space | 播放/暂停 |
| I / O | 标记起点/终点 |
| Enter | 添加有效区间到显示的目标 Block |
| Esc | 清除当前未提交区间；模态层优先处理 |
| 左/右方向键 | 精确帧模式下前/后一帧；未具备时明确标为小步定位 |
| Shift + 左/右 | 初始约定跳转 5 秒，可调整 |
| K / L | 暂停 / 向前播放；倍速提供可见控件 |
| J | 第一版明确标为后退定位；连续倒放后续实现 |
| B | 无完整区间时收藏播放点，有区间时收藏区间 |
| Q | 有效区间加入素材篮；无区间时提示先标记 I/O |

I/O 与快捷键习惯参考 [Premiere 默认快捷键](https://helpx.adobe.com/premiere/desktop/get-started/keyboard-shortcuts/default-keyboard-shortcuts.html)。不承诺浏览器原生负速率即可稳定倒放，负 playbackRate 支持有限，第一版明确区分后退定位与倒放：[MDN playbackRate](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/playbackRate)。

输入框、中文输入法组合、对话框和按钮原生键盘行为优先。漫画 Space 仍为其原有预览行为，视频 Space 为播放，使用既有快捷键作用域隔离。不注册截获剪映按键的全局快捷键。

设 In/Out 不立即截文件。只有一个端点时 Enter 不生效；Out 不大于 In 时原位提示，不静默交换端点。重新设置 In 使已有 Out 无效时保留提示，要求用户修正。

成功绑定后清除选区、保留播放位置和当前 Block，避免连续按 Enter 误加重复素材；用户重新明确选择相同区间时允许创建独立引用。切换来源清除未提交区间并提示，不能把上集区间套到下集。切换 Block 时也清除草稿并保持来源位置，避免选区意外落入另一段。

修改已有视频素材使用独立“调整片段”状态，显示保存/取消；保存保留 assetId，进入一次 Undo。播放位置是 UI 状态，拖动边界过程合并为一次最终业务命令，不把每次鼠标移动写入历史。

## 6. 数据模型与 schema 迁移

### 6.1 统一资产

```ts
type MediaAsset = ImageAsset | VideoAsset

interface ImageAsset {
  id: string
  type: 'image'
  sourceId: string
  order: number
  crop?: { x: number; y: number; width: number; height: number }
}

interface VideoAsset {
  id: string
  type: 'video'
  sourceId: string
  order: number
  startUs: number
  endUs: number
  videoStreamIndex: number
  audio: { mode: 'mute' | 'keep'; streamIndex: number | null }
  selectionBasis: 'frame' | 'time'
}
```

采用整数微秒表达项目区间，避免各处混用秒/毫秒；这是对总纲 startTime/endTime 示例的实施细化。旧总纲格式导入时明确按秒转换，不同时维护两套可写边界。区间统一为 `[startUs, endUs)`，长度为 endUs−startUs。

视频 Crop 第一版不开放。不能让通用组件无条件把 Crop 算法应用于视频；未来增加时需统一旋转、像素比例、空间 Crop 与时间选段的处理顺序。

### 6.2 视频来源

```ts
interface VideoSource {
  id: string
  mediaType: 'video'
  path: string
  fileName: string
  durationUs: number
  fingerprint: { size: number; mtimeMs: number; hash?: string }
  video: {
    streamIndex: number
    codec: string
    width: number
    height: number
    timeBase: string
    startPts: string
    avgFrameRate: string
    variableFrameRate: boolean | null
    rotation: number
    sampleAspectRatio: string
    color: object
  }
  audioStreams: Array<{ index: number; codec: string; language?: string; channels: number }>
}
```

probe 原始 PTS/timeBase 保留为精确字符串或有理数；界面时间从选定视频流的展示时间原点归零。音频流与视频流的起始偏移必须保留，不能各自归零后破坏同步。平均帧率不等于逐帧时间表，VFR 状态未知时不能当作恒定帧率。

代理、缩略图及帧索引属于可重建缓存，项目只记录必要关联，不把它们视为原始来源。项目恢复位置可扩展 `workspace.activeMediaTab` 与按 sourceId 记录的播放位置；修改归一化函数，避免保存时被现有固定 workspace 结构丢弃。

收藏和素材篮使用带类型的候选结构：image 保持原语义，video-point 保存 timeUs，video-range 保存起止与音轨策略。时间点不允许直接生成 BlockAsset；区间候选转入时创建新 assetId，默认保留候选。

### 6.3 迁移与持久化

按实施时 schema 从当前 2 顺序迁移到下一版本。已有无 type 资产显式补为 image，已有图片及 PDF Source 补 image 媒体类型，保留原 kind、ID、路径、Crop、录音及状态。遇到未知资产类型应明确报错或进入只读兼容态，不在保存时静默过滤。

Main 与 Renderer 使用同一版本的领域校验。创建和追加来源不能继续只复制当前图片字段；导入结果、ProjectService 和 Store 的视频元数据必须完成保存往返验证。

当前追加来源服务先从磁盘读取再保存。新增视频导入可能持续较久，应先探测来源，再向主窗口最新内存项目提交 additions 命令，并按修订号保存；不能把长任务开始时的磁盘快照整体写回，覆盖期间文案修改。重定位同样应遵循唯一项目写入链路。

## 7. 视频导入与预览兼容

多选文件先返回列表，逐文件 probe，显示可播放状态、时长、分辨率、音轨和处理进度。一个坏文件不阻止其他来源加入，失败条目可重试；重复选择不能新增重复来源。按自然文件名排序并允许手动调整剧集顺序。

ffprobe 使用受控 JSON 输出获取容器与流信息；不按 `.mp4/.mkv` 扩展名推断编码支持。选取真实视频流而非封面图，多个有效视频流时显示选择。参考 [ffprobe 官方文档](https://ffmpeg.org/ffprobe.html)。

播放路径分为：

1. 实际 Electron 构建可解码的来源直接预览。
2. 原文件可由随包 FFmpeg 解码，但播放器不可播放时，按需生成 SDR 720p 预览副本；显示进度、取消和空间占用。
3. 工具链也无法正确处理时保留明确失败原因，不让用户在黑屏上标记时间。

第一版支持清单应以 Windows/macOS 随包构建实测为准。MP4/H.264、MKV/H.264、HEVC、多音轨和无音轨分别验收；HDR、交错视频及损坏时间戳明确标记未支持，直到有正确转换链路。

不能把整集视频通过 `readFile → ArrayBuffer → IPC` 送到 Renderer。使用受控本地媒体协议/流式通道，支持浏览器需要的范围读取与 seek；路径由来源 token 解析，拒绝任意路径遍历。源文件导入后不复制原片，预览副本单独计入缓存。

## 8. 时间边界、逐帧定位与导出一致性

这是动漫模块最关键的正确性要求：用户选择的第一帧和最后一帧，应与交付片段一致。

### 8.1 三种时间必须分开

来源时间用于定位原视频；片段内时间从零开始；最终剪映时间由用户排列决定。素材时长之和不能自动当作旁白时长或成片时长，混入静态图片后更不存在天然总时长。

显示 `HH:MM:SS.mmm`，内部使用归零后的展示时间。原视频有非零起始 PTS、B 帧或可变帧率时，通过统一时间映射关联 UI、ffprobe、预览副本和导出。

### 8.2 O 点语义

按 I 选择当前显示帧的开始；按 O 表示包含当前显示帧，将 endUs 保存为下一帧展示起点，末帧则使用验证后的媒体结尾。UI 可以显示“末帧”，但持久化仍为右开区间。时间输入确认时吸附到实际可用帧边界并展示调整后的值。

普通播放可使用 video 元素。`currentTime` 是定位时间接口，`requestVideoFrameCallback` 提供展示帧的 mediaTime；二者本身不是完整的精确逐帧剪辑方案：[MDN currentTime](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/currentTime)、[MDN requestVideoFrameCallback](https://developer.mozilla.org/en-US/docs/Web/API/HTMLVideoElement/requestVideoFrameCallback)。

最终确认边界时读取附近帧时间索引并展示精确边界帧；VFR 不使用 `1 / avgFrameRate` 假装前后一帧。可按需解码短窗口获得帧图与 PTS，缓存附近结果，无需导入时扫描整集所有帧。

快速 seek 的旧回调必须带 source/session/request 序号丢弃。seek 未稳定时显示定位中并禁用确认，不把旧帧与新时间码绑定。浏览器循环回放可能受调度误差影响，精确选段确认依靠边界帧及导出结果校验。

### 8.3 预览副本的时间映射

预览副本必须记录其时间原点与原片映射，保持总时序；如果生成 CFR 副本引入重复/丢帧，最终边界回到原片帧索引确认，不能以副本帧序号直接裁原片。源文件改变时失效所有相关代理和索引。

## 9. 原声、真人旁白与字幕

“播放时听原声”和“交付视频是否带原声”是两个独立设置。默认播放原声帮助理解剧情，默认交付静音视频用于解说；卡片持续显示“交付：静音/保留原声”，用户可按片段修改。默认值属于本方案约定，可保存项目偏好。

多音轨来源必须允许明确选择，选择结果随素材保存，试听与交付使用同一音轨。无音轨视频允许正常静音交付；keep 指向不存在音轨时提示修正，不静默切换语言或补假音频。保持原有 A/V 偏移，导出不能自动把原声拉伸去追随旁白。

真人录音使用现有 Block voice 模型，不把原片音轨当作 active Take。开始麦克风录音前暂停并静音视频预览，避免外放串录；停止录音后恢复预览偏好但不自动开始播放。

第一版不自动烧录软字幕或导入 ASS/SRT，内嵌字幕轨明确标为未处理；画面本身已经压入的硬字幕无法通过关闭字幕轨消除。原片字幕、用户文案、剪映生成字幕三者分别处理，不自动互相替代。

## 10. 缩略图、收藏与 Used 区间

时间带按缩放级别请求所需缩略图，可采用总览约 10 秒一张、局部约 2–5 秒一张作为初始档位，生成当前视口优先，不用大量独立进程逐图遍历整集。总览目标是人工定位，不代表自动识别镜头切点。

缩略图缓存 key 包含来源指纹、视频流、时间戳、方向、尺寸和渲染版本。快速拖动取消低优先级过时请求；渲染失败仍允许直接时间定位，不阻断整个项目。

Used 索引按 sourceId 保存 `{blockId, assetId, startUs, endUs}`，时间带显示引用区间，重叠区域可展开对应 Block。显示“引用次数”和“覆盖时长”时分别计算：同一区间引用三次计三次，覆盖时长按区间并集，不把重叠时长重复相加。收藏和素材篮不计入已使用。

定位原片段打开对应剧集、暂停到 In 并高亮整个区间，不能立即覆盖现有绑定；编辑区间需显式进入调整状态。删除、复制、修改区间及 Undo/Redo 后同步索引。

## 11. FFmpeg 执行与片段生成

### 11.1 精确导出默认重编码

第一版交付 MP4/H.264、兼容像素格式，保留原比例、方向及正常播放速度；keep 模式输出已选择的 AAC 音轨，mute 模式不含音频轨。不同来源可保留各自尺寸和帧率时序，不为了统一项目而偷偷拉伸、变速或补固定画布。

视频宽高若不满足所选编码器约束，按明确规则最多补齐必要边缘像素并记录实际尺寸，不裁掉用户内容。优先保证首尾画面和时长正确，硬件编码作为验证后的可选优化。

任意 In/Out 默认解码裁切并重编码，不以 `-c copy` 承诺帧精确。FFmpeg 文档说明输入 seek 可能先定位到更早的可 seek 点，而转码的 accurate seek 与 stream copy 保留前段的行为不同：[FFmpeg seek 与转码文档](https://ffmpeg.org/ffmpeg.html)。快速无损关键帧裁切仅作为后续明确标注边界差异的可选模式。

实现统一导出计划：解析原片时间映射 → 定位前置解码点 → 精确 trim → 视频/音频同步时间归零 → 编码封装 → 校验。是否使用输入 seek 加速应由样本验证决定，不在 UI 暴露未经验证的 FFmpeg 参数。

默认保留可变帧率时序；若选定交付预设要求 CFR，清单记录转换规则和输出帧率，并按实际输出帧间隔评估误差。不得声称 CFR 转换后帧数与源逐一相同。

### 11.2 任务服务

Main 通过 `spawn(binary, args, { shell: false })` 运行固定可执行文件，参数由主进程白名单选项生成。Renderer 不提交命令文本、滤镜字符串或任意输出路径。任务记录所属项目、快照修订、源指纹、输入选段、输出参数和工具版本。

探测、缩略图、预览副本及交付编码共享调度器，当前播放器请求优先于后台总览。初始同时执行一个重编码任务，缩略图少量并发；根据实际机器调整，不同时转整季视频。

进度按任务阶段和媒体时间显示，编码完成不等于包发布成功。取消时停止排队、终止本任务进程并等待退出及句柄关闭，再清理有归属标记的临时文件；不执行按进程名终止全部 FFmpeg。

### 11.3 工具链交付

按实际支持的 OS/架构提供固定版本 FFmpeg/ffprobe，开发环境可配置受控路径，正式用户不应必须自行配置 PATH。启动时检测工具可执行性及必要编解码能力，缺失时显示修复说明。

构建任务记录二进制来源、版本、编译选项、架构和校验值，并按选定构建附带相应许可材料；FFmpeg 的许可条件与启用组件有关，不能把所有构建视为同一种许可：[FFmpeg 官方许可说明](https://ffmpeg.org/legal.html)。这是发布依赖工作，不应加入日常选段流程。

## 12. 混合故事板与素材包

故事板图片沿用现有预览；视频卡片显示封面、来源、In/Out、时长和原声策略，点击才播放选区。整页只允许一个前台视频播放器，离开卡片停止播放，避免几十个 video 实例同时解码。

增加 `INVALID_VIDEO_RANGE`、`VIDEO_STREAM_UNAVAILABLE`、`AUDIO_STREAM_UNAVAILABLE`、`SOURCE_CHANGED`、`UNSUPPORTED_VIDEO` 等检查。边界必须满足有限安全整数、0 ≤ startUs < endUs ≤ durationUs，至少包含一帧。恢复时未知状态不显示检查通过。

文案检查按 text/voice 模式区分：真人录音可以没有参考文案，无旁白段沿用现有显式规则。片段比旁白短只展示客观时长供用户判断，不阻断、不自动循环画面。

```text
Episode01/
├── images/
│   └── 001_01.png
├── videos/
│   ├── 001_02.mp4
│   └── 002_01.mp4
├── script.txt                  # 按旁白模式和现有导出选项决定
├── audio/                      # 真人录音模式
├── storyboard.html
└── project.json                # 固定快照 + exportManifest
```

Block/asset 序号来自一个混合导出计划，不能 images 和 videos 各自从 01 编号。当前 Block 数组顺序是 ExportPlanner 的项目顺序来源，继续遵循现有约定，asset order 仍校验连续性。

分开的 images/videos 目录不会自动形成跨目录的全局导入顺序。完成页明确提示在剪映素材区统一按名称查看；需要一批选择全部媒体时，可提供可选统一 media 目录导出布局，但一次包只采用一种布局，避免重复文件混入。任何布局都不承诺自动排列剪映时间轴。

manifest 增加 asset type、源 ID/指纹、原片范围、流选择、输出相对路径、实际时长、尺寸、帧率策略及编码版本。HTML 视频使用相对路径、poster、controls 和 preload=none，不自动播放；移动整个包仍可离线打开，文本与路径继续转义。

原子发布沿用 Phase 5：同一快照生成所有产物，全部成功后发布新目录。视频失败不得以跳过素材的方式标记整包成功；导出期间源文件改变须失败并重新检查，不能混合新旧源片段。

## 13. 视频拖出与稳定交付文件

沿用 Phase 6“准备好再拖出”，视频渲染通常更久，卡片显示准备进度和重试。未准备时提供准备按钮，完成后用户再次拖动，不在后台转码结束时突然发起系统拖拽。

当前 `registerAssistantIpc` 的 key 主要由项目/Block/asset/Crop 组成，未覆盖视频时间、音轨、源指纹和编码设置；扩大到视频前必须抽出完整 key 和进行中任务合并。异步任务开始时固定 session/项目/修订，完成时再次核对，不能把旧任务返回到切换后的项目。

```text
renderKey = hash(
  sourceFingerprint + videoStream + startUs + endUs
  + audioMode + audioStream + rotation + colorPolicy
  + outputProfile + toolVersion + renderVersion
)
```

可重建缓存与对外交付目录分开。交付 MP4 使用独立不可变版本路径；改 In/Out、改音轨或原文件改变时生成新版本，不能覆盖剪映可能已引用的旧文件。重复拖同一已生成版本复用路径，关闭窗口/应用、清理缩略图或代理都不删除交付文件。

视频 startDrag 的 icon 必须是生成的封面 PNG/nativeImage，不能沿用当前图片链路把 MP4 路径当图标。拖动由实际来源窗口发起，token 绑定窗口、会话及资产内容版本；准备失败、token 过时或文件缺失时不给出成功提示。

提供“打开交付目录”作为手动导入替代。发起拖动不代表剪映接收成功，不自动更新已放画面状态。辅助窗口继续只发送命令，主窗口维护唯一项目历史和保存权。

## 14. 模块与 IPC 设计

建议增量目录：

```text
src/features/video/
  components/VideoWorkspace.jsx
  components/VideoPlayer.jsx
  components/VideoTimeline.jsx
  components/VideoOverview.jsx
  hooks/useVideoSelection.js
  hooks/useVideoPlayback.js
  model/videoRange.js
  model/videoUsageIndex.js
src/features/assets/model/mediaAsset.js
electron/services/
  VideoProbeService.js
  VideoPreviewService.js
  VideoFrameService.js
  VideoRenderService.js
  MediaTaskScheduler.js
  MediaDeliveryService.js
electron/ipc/registerVideoIpc.js
```

媒体资产分发集中处理 validate/preview/locate/prepare/export，不让每个组件重复猜测扩展名。图片继续进入 AssetRenderer，视频进入 VideoRenderService，二者由统一交付服务协调，暂不做插件架构。

```ts
video.chooseSources()
video.probe(importToken)
video.getPlaybackSource({ projectId, sourceId })
video.requestThumbnails({ projectId, sourceId, range, width })
video.resolveBoundary({ projectId, sourceId, timeUs, direction })
video.preparePreview({ projectId, sourceId, profileId })
video.cancelTask(taskId)
video.onTaskProgress(callback) // 返回取消订阅

commands.addMediaAsset({ blockId, asset })
commands.updateVideoRange({ blockId, assetId, startUs, endUs })
commands.setVideoAudio({ blockId, assetId, audio })
```

输入需按窗口身份、项目会话、来源登记、时间范围和预算校验；resolveBoundary 使用固定枚举，不接受任意 FFmpeg 表达式。批量缩略图请求限制数量和像素预算。事件包含 taskId/sessionId/sourceId/revision，旧结果不能覆盖新页面。

## 15. 开发里程碑与完成门槛

| 里程碑 | 内容 | 必须通过后才能进入下一步 |
|---|---|---|
| Anime A1：统一媒体基础 | schema 迁移、来源类型、混合 assets、追加来源命令 | 老漫画/PDF/录音项目保存往返无损，视频字段不丢失 |
| Anime A2：选段闭环 | probe、可用预览、时间映射、I/O、精确边界、绑定与定位 | 一集视频可选取多段，重启恢复相同范围，Undo 有效 |
| Anime A3：交付闭环 | FFmpeg 精确渲染、原声策略、混合导出、HTML | 片段首尾与选区一致，在目标剪映版本可播放 |
| Anime A4：日常找素材 | 时间带、总览、收藏、素材篮、Used 索引 | 跨集找段、复用和回到原时间顺畅，不造成后台任务堆积 |
| Anime A5：辅助窗口与发布 | 视频准备/拖出、缓存寿命、打包及平台验收 | 重启剪映仍能读取交付素材，正式安装环境无需手配工具 |

总纲允许 Proxy 后置，但“播放器不能播放的输入如何反馈”必须在 A2 解决。完整代理管理可后移；若目标用户样本主要是浏览器不可解码的 MKV/HEVC，基础兼容预览副本就应前置到 A2，而不是让关键素材无法选段。

A3 完成后可进行受控试用，A5 后才称为动漫模块正式闭环。不要为先做漂亮总览而推迟片段正确导出。

## 16. 测试样本与验收

### 16.1 样本集合

准备具有可识别逐帧编号和同步声标的短视频，以及真实 24 分钟剧集。覆盖 23.976/24/25/29.97/30 fps、VFR、长 GOP/B 帧、非零起始时间、多音轨、无音轨、旋转/SAR、中文路径、大文件、损坏文件与被替换来源。未支持的 HDR/交错样本应得到明确阻断结果。

### 16.2 功能验收

| 场景 | 通过标准 |
|---|---|
| 旧项目迁移 | 图片/PDF Crop、录音、收藏及顺序完整，原 ID 不变 |
| 图片与视频混排 | 预览、排序、Undo、保存和导出顺序一致 |
| 导入时继续写稿 | 导入完成不覆盖期间正文修改 |
| I/O 和边界值 | 起点、末帧、单帧区间正确；反向/越界/未完成区间不能绑定 |
| 精确逐帧 | CFR 与 VFR 使用实际帧边界，显示帧与保存范围对应 |
| 快速 seek/切源/切项目 | 无旧帧错标、错段绑定或任务跨项目回写 |
| 定位与修改范围 | 跳回正确剧集和 In，修改保留 assetId，Undo 一步恢复 |
| 多音轨/静音 | 实际交付音轨与选择一致，静音输出不含音频轨 |
| 真人录音共存 | 视频不串录，voice 导出不新增字幕，不用原声音轨代替 Take |
| 导出片段 | 解码后首末画面与选定源帧一致；时长偏差不超过适用的一帧间隔 |
| A/V 同步 | 测试声标相对画面的偏移符合源时序和编码容差，无累积漂移 |
| 混合素材包 | 全局编号不重复，视频 HTML 不当作 img，离线可查看 |
| 取消/磁盘满/进程失败 | 无成功残包、无遗留运行任务、旧包和原片不变 |
| 拖出到剪映 | 实际 MP4 可导入、封面拖动正常；取消不勾选完成 |
| 修改后重开旧剪映工程 | 旧交付路径和内容仍在，新版本不覆盖旧素材 |
| 缓存清理/应用重启 | 可重建缓存可恢复，已交付文件不被清理 |

对重编码视频用解码后的帧标识或容差比较，不要求压缩像素逐字节相等；AAC 延迟按编码器实际元数据及解码结果验证。VFR 的“一帧”按对应边界帧间隔评估，不用平均帧率换算统一容差。

自动化覆盖区间运算、迁移、Used 并集、缓存 key、导出计划、音轨映射和任务取消；集成测试调用随包 FFmpeg 处理短样本。当前 pnpm test 仍是占位，实施时增加真实测试入口；pnpm build 和 pnpm start 分别用于构建及真实桌面流程验证。

### 16.3 性能与用户实践验证

以 12 集约 24 分钟视频、200 个 Block、500 次素材引用作为初始长项目样本。记录机器、系统、Electron/FFmpeg/剪映版本、编码类型、是否代理及冷/热缓存。目标：绑定/切段即时给出反馈，元数据未就绪也显示任务状态；重编码期间仍能写稿、暂停及取消；内存与任务数量受限，不按整季解码帧数无限增长。

邀请创作者分别完成“边看边写 10 段”“按已有文稿配 10 段”“跨集复用 3 个片段”“修改已交付片段再导入”。观察手动记录时间次数、外部截片工具切换次数、错绑定次数及首尾返修次数。目标是不用手写时间、不逐段打开外部截片工具、错段绑定为 0；耗时提升在建立原流程基线后报告。

## 17. 建议首先开发的可用切片

从一份已有漫画项目出发，追加一个实际可播放的动漫视频，在同一 Block 中保留原图片并加入 I/O 选段，保存重开后定位原时间，再导出一张图片和一个准确 MP4。

这个切片同时验证统一模型、项目兼容、选段体验和文件交付。确认可用后扩展缩略图总览、候选收集及原生拖出，才能使“漫画 + 动漫”成为同一个 Studio 的两种自然工作方式。

## 18. 本轮实施记录（2026-09-07）

本轮实现 A1 的数据与命令基础，尚未开放动漫播放器入口：

- schemaVersion 升级到 3；旧图片/PDF 补齐媒体类型，保留 Crop、ID、录音、状态、收藏及素材篮。工作区保留 activeMediaTab、videoPositions 和扩展字段。未知来源/素材类型及更高版本拒绝加载保存，不回退到旧备份造成隐性降级。
- Main/Renderer 共用媒体归一化与视频区间校验。来源创建保留 probe 元数据、流时间基与指纹；混合资产支持持久化。
- Store 提供 addMediaAsset、updateVideoRange、setVideoAudio。区间使用整数微秒和右开边界，修改保留 assetId，业务修改支持一次 Undo/Redo；复制 Block 时为资产生成新 ID。时间修改标为 time，尚不承诺逐帧精确。
- 图片/PDF 追加来源改为向最新内存项目提交 APPEND_SOURCES，通过现有自动保存链路持久化，支持批内去重、撤销和跨项目结果丢弃；移除旧追加来源 IPC，避免磁盘快照覆盖编辑中的文案。
- 图片绑定入口拒绝视频，故事板检查明确显示视频能力未开放，导出计划阻止视频误入图片交付流程。

验证入口为 `pnpm test`（Node 22.15+，本轮使用 Node 24）：覆盖迁移、真实磁盘往返、未来版本拒绝、导入期间编辑、重复来源、混排调整/撤销、复制身份、非法区间/音轨和导出阻断。生产构建输出至临时目录，避免改动现有 dist。真实 Electron 桌面交互仍待人工验收。

下一轮优先 A2：受控视频导入/probe、本地流式播放、I/O 与原片帧边界确认、绑定与定位；随后完成 A3 精确 MP4 交付。视频素材篮、缩略图和拖出仍按 A4/A5 推进。本轮未实现 FFmpeg、代理或视频渲染，不视为 A2/A3 完成。

## 19. 本轮实施记录（2026-09-07）：A2 精确边界帧确认 / 逐帧吸附

- 新增 `VideoFrameService`：用 ffprobe `-read_intervals` 读取目标时间附近窗口的真实帧时间（优先 `pts_time`，回退 `best_effort_timestamp_time`），ffprobe 按解码序输出，统一排序去重为展示帧起点表。按项目+来源缓存帧表并合并窗口覆盖；每次读取前校验来源指纹，原文件变化即失效；同一来源串行执行，缓存来源数与帧数均有上限。
- 新增 `video.resolveBoundary` IPC，方向为固定枚举 `frame-start` / `frame-end` / `prev-frame` / `next-frame`，并校验主窗口身份、来源登记与时间范围。I 吸附到当前展示帧起点；O 吸附到下一帧展示起点，末帧使用媒体结尾，持久化仍为右开区间。窄窗口看不到相邻帧或窗口内无帧时，自动加宽重试一次，仍失败则明确报错，不假装吸附。
- 播放器接入：I/O 标记自动吸附帧边界，吸附值与原始标记不同时提示"已吸附到帧边界"；两端均吸附成功时资产 `selectionBasis` 记为 `frame`，帧服务不可用时按时间降级标记（`time`），不阻断选段。←/→ 变为前/后一帧（帧中先吸附到当前帧起点，再逐帧移动），Shift+←/→ 保持 5 秒跳转；帧服务不可用时方向键退回 0.1 秒小步定位。定位/调整片段进入时按资产已有的 `selectionBasis` 同步吸附状态。
- 重构：ffprobe 二进制解析抽取为 `ffprobeBinary.js` 供探测与帧服务共用；`VideoPlaybackService` 抽出 `locate` 供播放地址与边界解析共用，避免绕过来源登记。
- 验证：`pnpm test` 覆盖帧时间解析、吸附语义（单帧区间、末帧结尾、首尾步进为空）、窗口覆盖缓存与加宽重试、IPC 输入校验；真实 ffprobe 25fps 样本 gated 测试通过。Electron 冒烟测试更新为帧吸附断言（`selectionBasis=frame`、吸附边界落在原始标记的一帧内），并真实跑通导入、seek、选段绑定、防重复、编辑保存与重开恢复。
- 剩余工作：A2 收尾的人工验收（真实 24 分钟剧集、VFR、多音轨、快速 seek 场景）仍待执行；A3 FFmpeg 精确渲染导出、A4 时间带/素材篮、A5 视频拖出未开始。

## 20. 本轮实施记录（2026-09-07）：A3 FFmpeg 精确渲染与混合交付

实现 A3 交付闭环，从“时间选段试用”进入“可导出精确 MP4 + 混合素材包”：

- 新增 `ffmpegBinary.js` 工具链发现（`COMISC_FFMPEG_PATH` / 随包 `media-tools/ffmpeg` / 开发回退路径），与 `ffprobeBinary.js` 同构。
- 新增 `VideoRenderService`：参数由主进程白名单生成，`spawn(..., {shell:false})` 执行；输入 `-ss` seek + 重编码 `libx264`（`yuv420p`、偶数化补边、`+faststart`），不使用 `-c copy`，实测首末帧与选区逐帧一致；`mute` 输出无音轨，`keep` 输出所选 AAC 音轨；`-progress pipe:1` 解析 `out_time_us`；渲染后 ffprobe 校验产物时长/流并按源指纹前后核对原文件未变；生成选区首帧 JPG 封面供 HTML poster。
- `ExportPlanner` 开放视频资产：统一 Block/资产序号，图片进 `images/`、视频进 `videos/`（如 `001_01.jpg` + `001_02.mp4` 同一段混排），视频复用 `validateVideoAsset` 校验（区间/视频流/音轨），删除旧的 `UNSUPPORTED_VIDEO` 阻断。
- `ExportService` 渲染循环按类型分发（sharp 图片 / VideoRenderService 视频），manifest 记录类型、源指纹、原片范围、流选择、音轨策略、实际尺寸/时长、编码器与工具版本；导出视频前预检 ffmpeg 可用性；取消会终止正在运行的 ffmpeg 子进程，临时目录按原原子发布规则清理，不残留残包。
- `ExportWriters` storyboard.html 增加 `<video controls preload="none" poster>`，视频不再当作 `img`，字幕/脚本/录音导出路径不变。
- `validateStoryboard` 不再把合法视频标为“交付未开放”，只对无效区间/流/音轨报具体代码。
- 验证：`pnpm test` 新增 `animeExport.test.cjs`——白名单参数、进度解析、混合计划字段纯测；真实 ffmpeg gated 集成测：逐帧编号源渲染 0.4–0.8s，首末帧灰阶核对与选区一致、mute 无音轨/keep 含 AAC、封面生成、完整混合素材包（storyboard/manifest/编号）、取消不发布残包。Electron 冒烟在含关键帧样本上通过，确认应用启动与 A2 链路未回归。
- 备注（A2 遗留观察，不影响 A3 交付）：A2 帧窗口用 ffprobe `-read_intervals`，其 seek 落在目标前最近关键帧并按关键帧起算窗口时长；对长 GOP（如仅开头一个关键帧）样本，靠后位置的 O 边界可能吸附错误（表现为并入媒体结尾）。A3 导出按已保存区间渲染并校验首末帧，不受影响；精确窗口读取可改为 ffmpeg `-ss/-t + showinfo` 有界解码，留给 A2 收尾单独处理。

## 21. 本轮修复记录（2026-09-07）：布局改版回归与选段/交付缺陷

对 `d23a02f`（A3）、`5381d8a`（优化 phase6）、`4fc8860`（A4）、`b15fde4`（优化布局）做提交审查后修复：

- `b15fde4` 移除了撤销/重做按钮且没有替代入口，快捷键也没有 Ctrl+Z。现恢复检查器内的撤销/重做按钮（带计数禁用），并在 App 增加全局 Ctrl/Cmd+Z、Ctrl/Cmd+Shift+Z、Ctrl+Y；输入框内不拦截，保留浏览器原生撤销。
- 同一提交还删掉了工作区切换（漫画/动漫）与旁白模式（文字/录音）两个下拉，且无替代入口，动漫工作区与真人录音实际上无法进入。现恢复头部工作区下拉（始终可见，同时恢复 A2 冒烟测试依赖的 `[aria-label="工作区"]`）与旁白下拉（xl 以上直接显示），并在「更多项目操作」菜单内提供旁白模式入口，保证窄窗口也可切换。
- `INSERT_TEXT_BLOCKS` 在“首段空白且无素材”时整体替换，会连带丢掉该段已录制的 Take。现仅当该段既无文案、无素材、也没有录音时才替换。
- 导出成功后的交付基线原用实时 `project.blocks` 打指纹，导出期间的项目变化会混入基线；现改为点击导出时固定快照，并按 jobId 只记录一次。
- 视频时间带的已用区间标题读不存在的 `asset.blockId`，一直显示「已用于 #」；改用 usage 映射里的所属 Block。
- 移除视频工作区「视频导出尚未开放」的过期文案（A3 已开放）。
- 拆分段落的游标原取自 `document.activeElement`，可能取到别的段落的光标，或在点按钮抢焦点的平台退化为从中间拆；改为按段落 ref 读取该段 textarea 的 `selectionStart`。
- 头部两个下拉菜单增加点击遮罩与 Esc 关闭。
- 检查器「查看下一段」在文字模式下原按录音维度兜底选段，现按当前旁白模式兜底。

验证：`pnpm test` 36 项 34 通过 2 跳过（新增“导入文稿保留已录音空段落”回归测试）；`pnpm build` 通过；Electron 实机脚本验证撤销/重做按钮与快捷键、输入框内不拦截、下拉点外关闭、按光标拆分、时间带标题、混合导出与交付记录；A2 冒烟测试在含关键帧样本上重新通过（仅截图步骤受环境 `capturePage` 影响）。
