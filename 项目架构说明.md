# 项目架构说明

> 本文档基于对仓库当前代码的扫描整理，用于快速了解目录职责与整体架构。
> 产品代号：CommentaryDesk / MangaDesk（本地优先的「漫画 · 动漫解说」素材工作台）。

---

## 1. 项目定位

一个 **Electron 桌面应用**：把「看原始素材 → 写文案 → 选画面 → 整理素材 → 导入剪映」之间的重复机械操作压缩掉。
它不替用户做创作判断，只做机械化搬运与整理：

```text
原始漫画 / 动漫素材
        ↓  人工理解剧情、自己写文案
文案 Block ←→ 图片 / 视频片段绑定
        ↓  分镜检查 + 配音录制
导出交付包（PNG / 视频片段 / 字幕 / 分镜表）
        ↓
剪映辅助窗口拖拽使用
```

核心领域对象是 **Block（文案块）**：每个 Block 承载一段文案、若干绑定的素材（图片裁剪区域或视频时间区间）、配音 take、以及五个进度状态（文案 / 素材 / 配音 / 剪辑 / 特效）。

---

## 2. 技术栈

| 层 | 技术 |
| --- | --- |
| 桌面外壳 | Electron 44（`main.js` + `preload.js`） |
| 渲染层 | React 19 + Vite 8，纯 JavaScript/JSX（无 TypeScript） |
| 样式 | Tailwind CSS 3 + Radix UI 原语（`src/components/ui/`） |
| 状态 | `useReducer` + Context（`src/store/`），带撤销/重做 |
| 图像处理 | `sharp`、`@napi-rs/canvas`（导出渲染）、`pdfjs-dist`（PDF 阅读） |
| 媒体处理 | 内置 ffmpeg / ffprobe（`resources/<platform>-<arch>/media-tools/`） |
| 打包 | `@electron/packager`（`scripts/package-app.cjs`） |

---

## 3. 目录总览

### 根目录

| 路径 | 作用 |
| --- | --- |
| `main.js` | **主进程入口**。创建主窗口、注册自定义 `studio-video://` 协议、实例化所有服务并注册 IPC、检查媒体工具链。 |
| `preload.js` | **预加载桥**。通过 `contextBridge` 把 `window.mangaDesk` 暴露给渲染层，是所有主进程能力的唯一出口。 |
| `index.html` | Vite 的 HTML 入口（标题 MangaDesk）。 |
| `vite.config.js` / `postcss.config.js` / `tailwind.config.js` / `components.json` | 构建与样式配置。`components.json` 为 shadcn 约定，指向 `src/components` 与 `src/lib/utils`。 |
| `package.json` | 脚本：`dev`（Vite 开发服务器）、`build`（构建渲染层）、`start`（构建后启动 Electron）、`test`、`package:check` / `package:app`。 |
| `*.md` | 产品与阶段文档：`CommentaryDesk_Development_Document_V1.0.md`（总纲）、`init.md`、`phase2~phase6*.md`、`refactor.md`、`voice-narration.md`、`动漫剪辑模块开发文档.md`、`产品布局优化方案.md`、`pdf阅读体验优化.md`。 |
| `dist/` | Vite 构建产物，**生成目录，不要手改**；Electron 通过 `loadFile('dist/index.html')` 加载。 |
| `resources/` | 随应用分发的资源。`resources/<darwin\|win32>-<arm64\|x64>/media-tools/` 存放固定版本的 ffmpeg / ffprobe 与 `manifest.json`；`README.md` 说明发布校验流程。 |
| `scripts/` | `package-app.cjs`：校验媒体工具链 → 构建渲染层 → 用 electron-packager 产出 `release/<时间戳>/`。 |
| `tests/` | `electronVideoSmoke.cjs`：需要真实 Electron + 视频样本的端到端冒烟脚本（通过环境变量传入）。 |
| `node_modules/`、`.pnpm-store/`、`.idea/` | 依赖与本地工具缓存，不参与源码。 |

### `src/` — 渲染进程（React）

```text
src/
├── main.jsx              入口：错误边界 + 路由分流（#/capcut 走剪映助手，其余走主工作台）
├── App.jsx               主工作台容器：编排视图、快捷键、素材状态、助手快照推送
├── index.css             全局样式与 workspace 三栏栅格
├── app/                  工作台外壳
│   ├── AppHeader.jsx     顶栏：项目名、保存状态、视图切换、导入/导出、助手入口
│   └── WorkspacePage.jsx 三栏布局：素材浏览 | 文案编辑 | 状态面板
├── components/ui/        Radix + Tailwind 的无业务基础组件（button / card / scroll-area）
├── features/             按领域拆分的业务模块（见下）
├── shared/               跨 feature 复用的桥接、领域模型、hook、工具
├── store/                项目状态、命令、撤销/重做
└── lib/utils.js          `cn()` 等样式工具
```

#### `src/features/` 领域模块

每个模块大致遵循 `components/`（视图）+ `model/`（纯函数逻辑）+ `hooks/`（状态编排）+ `services/`（外部资源访问）的约定。

| 模块 | 作用 |
| --- | --- |
| `project/` | 项目生命周期：欢迎页、项目中心（列表/重命名/归档）、自动保存、引导加载、导入、素材路径与项目的映射。 |
| `library/` | 素材浏览区：`MangaBrowser` 列表/网格、搜索、收藏视图；`UsageBadge` 显示素材被哪些 Block 引用并可跳转。 |
| `reader/` | 阅读器：`ReaderDialog`（翻页、缩放、裁剪选区、加入素材篮/当前块）、`PageMedia`（图片与 PDF 页渲染）、`PdfDocumentRepository`（PDF 文档缓存与释放）。 |
| `blocks/` | 文案块编辑：`BlockEditor`（文本编辑、拆分/合并、批量操作、排序、配音面板嵌入）、`BlockStatusPanel`（五个进度状态、素材列表、素材篮、撤销重做）。 |
| `assets/` | 素材绑定与呈现：`BlockAssetList`、`ScratchBasket`（素材篮）、`SourcePreview`（含裁剪预览）、`model/crop.js`（裁剪校验/归一化）、`model/usageIndex.js`（素材使用索引）。 |
| `video/` | 动漫剪辑：`VideoWorkspace`（In/Out 选段、逐帧定位、静音/保留原声、加入当前块）、`model/videoRange.js`（时间格式化与区间校验）、`VideoAssetSummary`。 |
| `voice/` | 配音：`VoiceRecorderPanel`（录制/暂停/续录/试听/裁剪/删除/恢复）。 |
| `storyboard/` | 分镜检查视图：按 Block 汇总素材与进度，`model/validateStoryboard.js` 产出问题清单（缺文案、缺画面、来源不可用等）。 |
| `export/` | 导出视图：导出预设、交付记录、导出进度、打开产物目录 / 分镜表。 |
| `capcut/` | 剪映辅助窗口：独立渲染的置顶小窗，展示当前 Block 文案与素材，支持复制文案、把 PNG/视频拖入剪映。 |

#### `src/shared/`

| 路径 | 作用 |
| --- | --- |
| `bridge/mangaDeskBridge.js` | 对 `window.mangaDesk` 的封装：统一 `{ok, data, error}` 拆包、抛出 `BridgeError`。业务层只用它，不直接碰 IPC。 |
| `domain/mediaAsset.js` | 素材/来源的归一化与校验（`image` / `video`、视频区间校验）。**主进程与渲染进程共用。** |
| `domain/projectNormalize.js` | 项目结构归一化与版本校验（当前 `schemaVersion: 3`），阻止降级保存。 |
| `hooks/useShortcutScope.js` | 局部快捷键作用域。 |
| `lib/ids.js`、`lib/pageNumber.js`、`lib/pathKey.js` | 纯工具：ID 生成、页码格式化、跨平台路径归一化键。 |

#### `src/store/`

| 文件 | 作用 |
| --- | --- |
| `ProjectStoreProvider.jsx` | Context + `useReducer`，对外暴露 `state`、派生 `blocks/currentBlock`、以及 `commands` 命令集合。 |
| `projectReducer.js` | 核心 reducer（最大单文件），处理全部项目变更、脏标记、修订号、保存状态。 |
| `projectActions.js` | 命令构造器（load / 文本 / Block 增删合并 / 素材与素材篮 / 配音 / 导出预设 / 保存状态…）。 |
| `history.js` | 撤销/重做栈（上限 100 步）。 |
| `projectSelectors.js` | 派生选择器。 |

### `electron/` — 主进程

```text
electron/
├── ipc/        IPC 注册层：把主进程服务能力暴露成通道
└── services/   业务服务层：文件、媒体、导出等真实工作
```

#### `electron/ipc/`

| 文件 | 作用 |
| --- | --- |
| `result.js` | 统一 `{ok, data}` / `{ok: false, error}` 返回包装，异常不跨进程抛出。 |
| `registerProjectIpc.js` | `project:*`：创建/保存/最近/列表/打开/重命名/归档/重定位来源。 |
| `registerSourceIpc.js` | `images:choose-directory`、`images:choose-pdf`、`pdf:read`、`sources:validate`。 |
| `registerExportIpc.js` | `export:*`：选目录、预检、启动、取消、打开产物目录与分镜表。 |
| `registerVideoIpc.js` | `video:*`：选视频、播放授权、区间边界解析；仅主工作台可访问。 |
| `registerVoiceIpc.js` | `voice:*`：录制会话的 start/append/pause/resume/finish/discard、读取、回收与回收站。 |
| `registerAssistantIpc.js` | `assistant:*`：剪映助手窗口的打开、快照同步、命令下发、素材准备与取消、拖拽令牌。 |
| `registerSystemIpc.js` | `system:copy-text`（剪贴板）。 |

#### `electron/services/`

| 文件 | 作用 |
| --- | --- |
| `ProjectService.js` | 项目持久化：写入 `userData/projects/<id>/project.json`，原子写（`.tmp` + `.bak`）+ 每项目串行队列；维护 `recent-project.json`。 |
| `SourceScanService.js` | 扫描目录图片（jpg/png/webp/gif/bmp）与 PDF 页列表。 |
| `ExportPlanner.js` | 导出计划：文件名清洗、素材路径解析、按 Block 生成导出任务与校验问题。 |
| `ExportService.js` | 导出作业编排：预检、进度回调、取消、产物目录、幂等与缓存。 |
| `AssetRenderer.js` | 图片/PDF 页裁剪渲染为 PNG（sharp + canvas），带指纹缓存。 |
| `ExportWriters.js` | 写出剪映导入脚本、字幕、`storyboard.html`。 |
| `NarrationExportService.js` | 配音 WAV 读取、裁剪校验与导出（48 kHz 单声道 16 bit）。 |
| `VideoProbeService.js` | 用 ffprobe 解析视频流信息、帧率、时长。 |
| `VideoFrameService.js` | 逐帧/边界帧提取与缓存（帧起点表、有限容量缓存）。 |
| `VideoRenderService.js` | 用 ffmpeg 渲染视频片段（H.264 / AAC / PNG）。 |
| `VideoPlaybackService.js` | 通过自定义 `studio-video://` 协议做支持 Range 的视频流式播放。 |
| `MediaDeliveryService.js` | 剪映助手的素材交付：按内容版本渲染并缓存可拖拽文件，取消与失效控制。 |
| `MediaToolchainService.js` | 校验打包内的 ffmpeg/ffprobe 清单、哈希与编解码能力。 |
| `ffmpegBinary.js` / `ffprobeBinary.js` | 二进制定位：打包内优先，开发环境可用 `COMISC_FFMPEG_PATH` / `COMISC_FFPROBE_PATH` 或系统路径。 |
| `VoiceRecordingService.js` | 录音会话管理：分片写入、暂停/续录、恢复可回收会话、回收站。 |
| `AssistantWindowService.js` | 剪映助手窗口生命周期与置顶偏好持久化。 |

---

## 4. 分层架构

```text
┌──────────────────────────── 渲染进程 (React) ─────────────────────────────┐
│  App.jsx ── app/ 外壳 ── features/* 业务模块                              │
│     │                                                                     │
│     └── store/  (useReducer + commands + undo/redo)                       │
│     │                                                                     │
│     └── shared/bridge/mangaDeskBridge.js   ← 唯一 IPC 出口，统一错误处理    │
└──────────────────────────────────┬────────────────────────────────────────┘
                                   │  contextBridge (window.mangaDesk)
                                   │  ipcRenderer.invoke / on
┌──────────────────────────────────┴────────────────────────────────────────┐
│  preload.js                     （无 Node 能力泄漏到页面）                  │
└──────────────────────────────────┬────────────────────────────────────────┘
                                   │  ipcMain.handle
┌──────────────────────────────────┴────────────────────────────────────────┐
│  electron/ipc/*  注册层：鉴权、参数校验、result() 包装                      │
│  electron/services/*  服务层：文件 IO、ffmpeg/ffprobe、sharp、导出编排      │
│  main.js  窗口 / 协议 / 服务装配                                           │
└───────────────────────────────────────────────────────────────────────────┘
```

**设计约束**

- 浏览器侧代码只放在 `src/`；Electron API 不得直接进入 React 组件，一律经 `preload.js` + IPC。
- 主进程不抛异常给渲染层，统一 `result()` 包装为 `{ok, data|error}`，`mangaDeskBridge` 负责拆包。
- `src/shared/domain/` 的归一化/校验逻辑被两侧共用，保证前后端对项目结构的理解一致。

---

## 5. 主要数据流

**素材导入 → 绑定 → 交付**

1. 用户选目录或 PDF → `registerSourceIpc` → `SourceScanService` 扫描出页/图列表。
2. 渲染层 `hydrateSources` 把路径映射成 `sourceId`，`commands.appendSources` 写入项目。
3. 在阅读器/视频工作区选中画面或时间区间 → `addAssetToCurrentBlock` 绑到当前 Block。
4. `StoryboardView` + `validateStoryboard` 做交付前检查。
5. `ExportView` → `export:preflight` / `export:start` → `ExportPlanner` 计划 → `AssetRenderer` / `VideoRenderService` / `ExportWriters` 产出 PNG、视频片段、字幕、分镜表。

**保存**

状态变更在 reducer 里递增 `revision` → `useProjectAutosave` 防抖触发 `project:save` → `ProjectService` 归一化后原子写盘。

**剪映助手**

主窗口把当前项目快照 `assistant:publish` 给主进程 → 助手窗口（`#/capcut`）订阅 `assistant:snapshot` 渲染 → 需要素材时 `assistant:prepare` 让 `MediaDeliveryService` 渲染并缓存文件 → 返回令牌供拖拽，`assistant:command` 反向更新 Block 状态。

---

## 6. 数据模型要点

- 存储位置：`<userData>/projects/<projectId>/project.json`，另有 `<userData>/recent-project.json` 记录最近项目。
- `schemaVersion: 3`；`projectNormalize.js` 会拒绝保存比当前支持版本更高的项目。
- `project.sources[]`：来源（`mediaType: image | video`，图片可为 PDF 单页）。
- `project.blocks[]`：文案 + `assets[]`（图片裁剪区或视频时间区间）+ `voice`（takes、裁剪、间隔、是否必须配音）+ `status`（五个进度布尔）。
- 另有 `favorites`、`scratchBasket`、`exportPresets`、`deliveries`、`videoFavorites`、`workspace`（当前块/来源、媒体 Tab、视频播放位置）。

---

## 7. 开发与验证

```bash
pnpm install          # 安装依赖
pnpm dev              # 仅启动 Vite 开发服务器（渲染层热更新）
pnpm start            # 构建渲染层后启动 Electron（完整联调）
pnpm build            # 产出 dist/
pnpm test             # node --test tests/*.test.cjs
pnpm package:check    # 校验 resources 下媒体工具链
pnpm package:app      # 产出 release/<时间戳>/
```

**注意事项**

- `dist/` 是生成产物，除非改动确实需要构建结果，否则不要提交。
- 视频相关功能依赖 ffmpeg/ffprobe：开发环境需安装或设置 `COMISC_FFMPEG_PATH` / `COMISC_FFPROBE_PATH`，否则启动时会弹提示（图片与文案功能仍可用）。
- 应用名保持 `Electron` 以复用默认 userData 目录，改名会导致旧项目失联。
- 测试覆盖偏薄：`tests/` 目前只有视频冒烟脚本；新增自动化时优先覆盖 `src/**/model/`、`src/store/`、`electron/services/` 中的纯函数。
- 代码风格：两空格缩进、单引号、尽量少分号；组件 `PascalCase`，hook 用 `useThing`，工具/服务用 camelCase。
