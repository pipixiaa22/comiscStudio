# MangaDesk Phase 4 开发文档

> 主题：收藏、临时素材篮、图片使用标记与 Storyboard  
> 依据：`init.md` 第 20–26、38–43、44 节  
> 前置条件：Phase 3 已完成整页/Crop 绑定、素材排序、原图定位、保存及撤销重做  
> 阶段目标：用户可以暂存候选画面、了解素材使用情况，并按完整剧情检查文案与配图。

## 1. 范围与阶段边界

| 本阶段交付 | 交付结果 |
|---|---|
| 收藏 Favorites | 长期保留值得再次查看的漫画页 |
| 临时素材篮 Scratch Basket | 暂存当前创作候选整页或 Crop，随后加入指定 Block |
| Used 标记 | 显示原页被引用次数，并跳转引用它的 Block |
| Storyboard | 按 Block 顺序展示全文与实际配图，检查客观缺项 |
| 状态汇总 | 展示文案、素材等人工完成状态，不推断创作质量 |

导出图片、`script.txt` 和离线 `storyboard.html` 属于 Phase 5；剪映辅助窗口、Native Drag 属于 Phase 6。本阶段 Storyboard 是应用内视图。继续使用项目 JSON、现有 Electron/React 架构和状态层，不以迁移 SQLite 或重写技术栈为前置条件。

以下未由 `init.md` 明确规定的细节，是本开发文档的实施约定：收藏以整页去重、素材篮支持 Crop、转入 Block 默认复制、素材篮随项目保存、使用次数按素材引用数计算。

## 2. 核心工作流

1. 阅读漫画，按 B 收藏以后可能有用的页面。
2. 找到近期候选画面，按 Q 将整页或当前有效选区加入素材篮。
3. 选中目标 Block，将素材篮条目拖入，或点击“加入当前 Block”。
4. 在原页 Used 标记中查看已经使用它的段落，点击段落回到文案。
5. 打开 Storyboard，按剧情顺序浏览全文与配图。
6. 从缺项提示跳回对应 Block 修正，再返回原故事板位置。

任何收藏或暂存动作都不自动创建 BlockAsset；只有实际加入 Block 才增加使用次数。

## 3. 数据模型与兼容

沿用 Phase 3 的 `sources`、`blocks[].assets`、稳定 `sourceId` 和归一化 Crop。图片文件、PDF 的每一物理页统一视为 Source；同一 PDF 的不同页使用不同 ID。

项目增加两个持久化字段：

```ts
interface FavoriteEntry {
  sourceId: string
  createdAt: number
}

interface BasketItem {
  id: string
  sourceId: string
  crop?: { x: number; y: number; width: number; height: number }
  order: number
  createdAt: number
}

// 增加到已有 Project；不替换原字段。
interface Phase4Fields {
  favorites: FavoriteEntry[]
  scratchBasket: BasketItem[]
}
```

类型示例仅表达合约，可在现有 JavaScript 项目中使用 JSDoc 和运行时校验实现。

迁移应注册为“当前 schemaVersion → 下一版本”的顺序迁移，以实施时已有版本号为准，不强制写回 1。旧项目缺失上述字段时补 `[]`，原 ID、文案、素材、Crop 和状态保持不变。遇到更新版本项目时阻止破坏性降级保存，给出版本提示。

收藏唯一键为 `sourceId`；素材篮唯一键为 `sourceId + 规范化 crop`，整页使用单独哨兵值。相同候选重复加入时定位已有条目并提示“已在素材篮”，不同 Crop 可以并存。沿用 Phase 3 的坐标精度与校验方法。

素材篮的“临时”指用途，不代表退出即清空。它与收藏均为项目级数据，不跨项目混用。Used 索引、检查结果、搜索过滤和悬浮菜单状态为派生/UI 数据，不落盘。

## 4. 收藏 Favorites

### 4.1 操作规则

- 当前页面按 B 或点击收藏按钮切换收藏，页面显示星形状态。
- B 始终作用于整页，即使存在 Crop 草稿；需要暂存 Crop 时使用 Q。
- 收藏面板按收藏时间倒序展示，可预览、定位原页、取消收藏以及加入当前 Block。
- 取消收藏只移除收藏记录，不删除原图、素材篮条目或 BlockAsset。
- 当前没有页面时禁用；失效来源保留收藏并展示不可用状态，仍允许取消收藏。
- 不增加标签、分类目录或跨项目收藏系统。

### 4.2 面板与反馈

漫画浏览器提供“全部 / 收藏”切换，复用页面卡片和搜索能力。收藏为空显示“看到想留用的画面，按 B 收藏”。在收藏视图取消收藏后，选择邻近条目；最后一项移除后进入空态，不跳到任意漫画页。

## 5. 临时素材篮 Scratch Basket

### 5.1 收集候选

Q 在浏览器/Viewer 范围生效：存在有效 pending Crop 时加入该 Crop；否则加入整页。选区无效时提示修正，不悄悄改为整页。成功暂存 Crop 后清除草稿，显示“已加入素材篮”。

素材篮展示实际裁切预览、来源页码、顺序和移除按钮，支持排序。默认位于可折叠面板，折叠后仍显示候选数量。

### 5.2 转入 Block

拖拽时目标 Block 明确高亮，放下时使用目标 `blockId`，不可依赖拖动开始时的全局当前 Block。提供“加入当前 Block”按钮作为键盘替代。

转入采用复制语义：创建新 `BlockAsset.id`，追加至目标素材末尾，候选保留以便复用；提示“已加入 #018”，可撤销。重复转入允许生成多个引用，与 Phase 3 的素材规则一致。

转入前再次校验目标 Block、来源和 Crop。目标已删除或跨项目拖动时取消；不创建悬空引用。应用内拖拽载荷只包含项目 ID 与条目 ID，不作为原生文件拖出。

### 5.3 移除与清空

移除候选不会删除其已经产生的 BlockAsset。提供清空按钮，明确显示条目数，清空作为一条可撤销命令；不随加入 Block 或关闭应用自动清空。

## 6. Used 标记与引用索引

### 6.1 统计口径

```ts
type UsageIndex = Map<string, {
  count: number
  references: Array<{ blockId: string; assetId: string }>
}>
```

遍历所有 `blocks[].assets`，每个有效 sourceId 引用计一次。同一 Block 对同一页使用两次计 2，整页与 Crop 均计入对应 Source；收藏、素材篮、单纯预览不计数。

显示约定：0 次不显示；1 次显示 `●`；多次显示 `●N`。悬停或键盘激活展开引用列表，按当前 Block 顺序分组，例如 `#018 · 2 次`、`#031 · 1 次`。编号由实时 order 计算，不能把编号当身份。

### 6.2 同步规则

增加/删除素材、复制/删除 Block、Undo/Redo 后更新统计；素材排序不改变总次数。删除全部引用后标记消失。缺失文件的已有引用仍计数，另外显示来源不可用，不隐瞒既有使用关系。

点击引用项切到工作区相应 Block，并选择相应素材；多条引用可展开到 asset 层级。触发 Used 控件不能冒泡成卡片双击添加。首次加载可全量构建，后续按项目修订号 memoize 或增量更新，禁止每个缩略图单独遍历全部 Block。

## 7. Storyboard 应用内视图

### 7.1 结构与导航

顶部提供“Storyboard”入口，视图可对应 `/storyboard/:id`；若现有项目使用视图状态而非路由，保持既有方式即可。进入视图使用当前内存项目，不能先读取可能滞后的磁盘快照。

每张 Block 卡片包含当前编号、完整文案、按 asset.order 排列的整页/Crop 预览、素材数量、完成状态、客观问题提示以及“编辑此段”。长文案保持换行，不用摘要代替全文。缺失素材保留原序号占位，不挤掉原排序。

点击“编辑此段”返回指定 Block；查看素材可打开已有大图或定位原页。返回 Storyboard 时恢复原卡片、滚动位置与问题筛选。Phase 4 不另建第二套文案编辑器，不增加时间轴、播放时长或自动节奏判断。

### 7.2 检查规则

| 问题码 | 判断 | 提示/修复入口 |
|---|---|---|
| EMPTY_TEXT | `text.trim()` 为空 | “缺少文案”，编辑此段 |
| EMPTY_ASSETS | assets 数量为 0 | “未选择画面”，回到工作区配图 |
| SOURCE_NOT_FOUND | sourceId 无对应 Source | “素材引用不存在”，定位对应素材记录 |
| SOURCE_UNAVAILABLE | 文件缺失、不可读或 PDF 目标页不可用 | “原文件/页面不可用”，显示来源及重新检查 |
| INVALID_CROP | 非有限数、非正尺寸或坐标越界 | “裁切范围无效”，回到原页重新框选 |

所有字段先做结构校验。合法旧数据不做静默修剪；只有新建选区的微小浮点边界误差可按 Phase 3 约定规范化。未知/检查中的状态不能显示为“检查通过”。

允许筛选“全部 / 有问题”，并按问题类型过滤。一个 Block 可有多个问题，汇总同时显示“有问题的 Block 数”和“问题条数”，避免混淆。空文案/空配图不阻止进入 Storyboard，不自动删空 Block；原图缺失也不删除项目引用。

### 7.3 检查执行

文案、素材和 Crop 结构检查在本地纯函数内完成；文件存在性/PDF 页有效性通过主进程受限服务批量验证，返回 sourceId 对应状态。PDF 同文件先共用一次文件检查和文档元数据读取，避免每个引用重复打开。

进入时运行检查，提供“重新检查”。项目改变后增量刷新，外部文件状态带检查时间。异步结果绑定项目 ID 和修订号，过时结果不覆盖当前项目。实际 Phase 5 导出前仍需再次校验源文件。

## 8. 项目完成状态

沿用 `scriptDone / assetDone / voiced / edited / effectDone`。汇总分母为当前全部 Block 数，分子为对应布尔字段为 true 的数量；以“文案 62/64”等形式展示，标记这是人工状态。

状态与质量检查独立：人工标记素材完成但文件后来丢失，仍展示完成标记与缺失警告。不得把“没有检查问题”自动等同于文案或素材完成。修改文案及删除最后一张素材时，沿用前阶段的完成状态重置规则。

## 9. Actions、持久化与键盘

建议增加以下业务 action，统一进入现有命令/历史层：

```ts
toggleFavorite(sourceId)
addBasketItem({ sourceId, crop })
removeBasketItem(itemId)
reorderBasket(itemId, toIndex)
clearBasket()
addBasketItemToBlock({ itemId, blockId })
setBlockStatus({ blockId, key, value })
```

收藏、素材篮增删排序/清空、转入 Block、状态勾选各形成可撤销命令；转入一次仅一条历史记录。去重后无变化的操作不进入历史。选择、筛选、展开面板、跳转和检查不进入 Undo 栈。

沿用 300–500ms 自动保存、手动保存、原子写入和错误重试。Renderer 与 Main 的项目归一化均须保留新增字段及现有 assets。重启恢复收藏与素材篮，历史栈是否跨重启保持前阶段约定。

B/Q 只在浏览区域且无输入焦点时响应；`input/textarea/contenteditable`、中文输入法组合及模态控件优先。Delete 只移除当前聚焦面板的选中条目，不能同时删除 Block 素材。所有快捷操作均配有可见按钮与清晰焦点。

## 10. 模块建议与实施顺序

复用 Phase 3 的 Source、Crop 校验、素材预览和定位服务。建议新增 `FavoritePanel`、`ScratchBasket`、`UsageBadge`、`UsagePopover`、`StoryboardView`、`StoryboardBlockCard`、`ProjectProgress`，业务层分出 `usageIndex` 与 `validateStoryboard` 纯函数。

1. 增加项目字段、迁移和保存往返测试。
2. 实现收藏与筛选。
3. 实现素材篮收集、复制转入 Block、排序及撤销。
4. 构建使用索引与引用跳转。
5. 实现 Storyboard 列表、素材预览与往返导航。
6. 接入客观检查、批量来源验证与状态汇总。
7. 优化可见区渲染、键盘作用域与错误反馈。
8. 按验收用例完成测试，为 Phase 5 暴露共用验证服务。

## 11. 测试与验收

| 测试 | 通过标准 |
|---|---|
| 旧项目迁移 | Block、素材、Crop、来源 ID 不变，新增字段补空 |
| 收藏切换与重启 | 无重复收藏，重启恢复，取消不影响已用素材 |
| 素材篮去重 | 相同来源/Crop 不重复，不同 Crop 可并存 |
| 转入指定 Block | 新建素材 ID、顺序正确、候选保留、Undo 一步恢复 |
| Used 口径 | 同页引用 3 次显示 3；收藏/暂存不计；撤销后同步 |
| Block 复制与排序 | 引用次数更新，弹层编号按新顺序显示 |
| Storyboard 内容 | 全文换行、Crop 与工作区一致，缺失素材保留占位 |
| 五类检查 | 构造对应问题均可发现，修复后重新检查消失 |
| 视图往返 | 编辑指定 Block 后返回原故事板位置，无丢稿 |
| 快捷键 | 中文输入、按钮激活和不同面板删除互不干扰 |
| PDF | 不同页独立收藏/计数/预览，缺页检查不误指其他页 |

性能验收建议使用 1000 个 Source、200 个 Block、2000 个素材引用的样本，记录机器与窗口条件。只有可见卡片及少量缓冲挂载高成本预览；收藏切换和已缓存的引用跳转目标反馈时间 ≤ 200ms。该值为开发目标，需实测确认。

完成定义：用户能收藏与暂存候选、可靠配到指定 Block、查看真实使用关系，并通过 Storyboard 找到和修复客观缺项；上述数据自动保存且撤销有效，Phase 3 功能无回归。

## 12. 交给 Phase 5 的合约

输出仍是同一份 Project，不引入独立故事板项目。Phase 5 复用 Source 解析、Crop 校验、Block/素材排序和检查结果结构；收藏及素材篮不自动成为导出素材。导出应对某一项目修订版生成快照，并重新验证文件状态。
