# MangaDesk PDF 阅读与选图体验优化方案

日期：2026-09-06  
定位：产品需求与交互规格，供设计、开发和验收使用  
依据：用户提供的当前左栏截图、当前 `src/App.jsx`、`main.js`、`electron/services/ProjectService.js`，以及 `init.md`、`phase2.md`、`phase3.md`

## 1. 产品结论

建议采用“缩略图定位 + 大图快速查看 + 可切换的边看边写布局”。第一批优先升级已有的大图预览，让用户点击一个明确按钮即可看清页面，在其中放大、移动、翻页，关闭后接着写稿。

当前最直接的问题是竖版漫画被放进低矮的横向预览区域。截图中该区域约为 490 × 220 显示像素，竖页实际宽度约 150 像素，左右大面积留白。预览虽然比缩略图高一些，却仍不足以辨认对白与小格细节。代码使用固定 `h-44`，说明预览高度不会随可用工作空间充分增长；截图尺寸还可能受系统缩放影响，不能当作 CSS 像素的准确测量。

优化应同时解决两件事：给页面足够的阅读空间，以及让 PDF 在放大后按显示需要重新渲染。只把低分辨率画布拉大，细节仍可能模糊。

## 2. 证据与评估边界

本次是基于用户截图与当前源码的定向体验分析，没有运行完整桌面流程，也没有开展用户测试。以下明确区分截图事实、代码发现和待验证判断。截图仅覆盖应用左侧，不能据此判断整个窗口尺寸或右侧布局是否溢出。

| 流程步骤 | 当前状态 | 证据与结论 |
|---|---|---|
| 1. 导入 PDF、找到页面 | 已具备基础能力 | 截图显示 196 页及页码缩略图；主进程将 PDF 按物理页展开。导入速度、失败恢复未实测 |
| 2. 选择需要查看的页面 | 选中反馈清晰 | P003 有橙色边框，上方同步显示相应页面；现有深色界面与选中样式可继续沿用 |
| 3. 看清对白与画面细节 | 主要阻碍 | 截图中的竖页非常窄；`Browser` 使用固定高度预览 |
| 4. 进入大图、连续阅读 | 入口隐蔽，工具不足 | 代码已有 Space 大图层，但没有可见入口、缩放和平移工具；点击层内任意位置即关闭 |
| 5. 看完继续写稿或配图 | 需要明确交互与数据保障 | 焦点与阅读位置恢复需补充；素材保存和 Crop 链路存在源码可见缺口 |

已有基础值得保留：按页展开、页码标识、缩略图检索、选中反馈、文案工作区，以及复用 PDF 文档实例的机制。无需为了阅读优化重新设计导入系统。

## 3. 用户真正要完成的三类任务

### 3.1 临时确认细节：本次首要场景

用户正在写一个 Block，看到某页后想确认人物表情、一句对白或某个分镜。理想过程为：选中页 → 点击“放大查看” → 放大目标区域 → Esc 返回 → 接着写。

目标是用户不需要知道快捷键，也不需要离开 MangaDesk。

### 3.2 连续阅读剧情

用户需要读前后十几页再组织文案。应能在大图模式内连续翻页，保留“适合宽度”等阅读偏好，不必每页关闭预览、再找缩略图。

### 3.3 边读边写、选取画面

用户频繁核对漫画和文案，需要让大图与编辑器同时可见。后续提供阅读布局，在大图中选取整页或框选区域，明确加入哪个 Block，并获得可见反馈。

这三类场景的使用频率尚未测量。按用户当前描述，先交付临时看清细节，再验证是否需要把阅读布局设为默认。

## 4. 方案取舍与推荐顺序

| 方案 | 适合解决什么 | 代价或限制 | 决策 |
|---|---|---|---|
| 单纯加高左栏顶部预览 | 轻微改善快速查看 | 仍受左栏宽度限制，并挤占缩略图空间 | 仅作为辅助调整 |
| 悬停自动出现大图 | 快速扫图 | 容易遮住文案、误触，对键盘用户不友好 | 不作为默认入口 |
| 升级现有应用内大图层 | 临时看清细节、连续翻页 | 打开期间遮住编辑器 | P0 首选 |
| 可切换阅读布局 | 长时间边看边写 | 需要布局切换与状态恢复 | P1 |
| 独立阅读窗口 | 双屏、长期对照 | 多窗口焦点和项目同步复杂 | P2，按需求验证 |

推荐保留当前缩略图选页习惯，以清晰按钮补足大图入口。第一批不要改变全部页面结构，先让当前用户更容易发现并完成“看清这一页”。

## 5. P0：把大图查看做成可发现、可操作、可返回的阅读器

### 5.1 入口与旧操作兼容

左栏顶部预览增加常驻工具行：当前 PDF 名称、`第 3 / 196 页`、`放大查看`。文件名过长时截断，悬停或聚焦可查看完整名称。

缩略图卡片在悬停及键盘聚焦时显示“放大查看”按钮；大图入口不能只存在于悬停态，顶部常驻按钮是主要入口。点击该按钮须阻止事件冒泡，避免同时触发加入素材。

沿用现有操作约定：单击缩略图选页，双击加入当前 Block，Space 打开/关闭大图。由于 `phase3.md` 和现有实现已将双击用于添加素材，本轮不把它悄悄改成预览；在卡片提示中说明“双击添加 · Space 查看”。

### 5.2 大图层布局

大图层覆盖应用工作区，默认使用窗口内最大可用区域。顶部工具栏高约 48 CSS px，主体占据剩余空间；外边距建议 16–24 CSS px，避免过度装饰消耗阅读面积。

| 区域 | 内容与行为 |
|---|---|
| 顶栏左侧 | PDF 文件名、当前页/总页数 |
| 顶栏中部 | 上一页、页码输入、下一页、缩小、缩放百分比、放大、适合整页、适合宽度 |
| 顶栏右侧 | 关闭按钮，明确显示 Esc 提示 |
| 阅读区 | 原比例页面、可滚动和可平移的画面 |
| 底部轻量提示 | 按当前状态显示“滚轮上下阅读 · Ctrl + 滚轮缩放”等，不遮住对白 |

首次打开默认“适合整页”，便于快速判断整页内容。用户切到“适合宽度”后，在本 PDF 本次会话中继续沿用。缩放值不能硬编码为 100%。

### 5.3 缩放与移动

- `适合整页`：完整显示页面，保持比例。
- `适合宽度`：页面占满阅读区域宽度，竖向滚动读取细节。
- `+ / -`：围绕视口中心逐步缩放；建议初始范围 25%–400%，极端尺寸受渲染预算限制时明确反馈。
- `Ctrl + 滚轮`：围绕鼠标所指位置缩放，人物或对白不应突然移出视野。
- 普通滚轮：在当前页上下滚动，滚到页尾也不自动跳页，减少误翻页。
- 阅读模式下左键拖动：平移放大后的画面；有可移动内容时显示抓手光标。
- PDF 中“100%”指应用定义的基础页面显示比例，不承诺对应扫描图片的原始像素精度。

按钮必须始终可用作键盘、触控板手势的替代入口；不要把鼠标快捷操作当作唯一入口。

### 5.4 翻页与定位

大图内提供可见的上一页/下一页按钮，支持 `← / →` 和 A/D。第一页禁用上一页，末页禁用下一页，不循环跳转。

页码输入显示 PDF 物理页号，支持直接输入 128 后回车跳转；空值、非整数、越界值在输入框附近提示，不用弹窗。页码输入聚焦时，Enter 用于跳页，不加入素材。

翻页沿用阅读模式，新页默认从顶部开始；如返回本会话刚看过的页面，优先恢复该页缩放与阅读位置。大图层选页与左栏当前页同步，关闭后选中最终查看的页面。

### 5.5 关闭与返回

提供明显关闭按钮和 Esc；点击画面、拖动结束或滚动不得关闭阅读器。P0 默认也不通过点击黑色空白区关闭，防止平移到边缘时误退。

打开时记录当前 Block、文本选区、编辑区滚动位置和入口元素。关闭时不自动完成 Block，不移动文案插入位置；焦点返回打开按钮或原先的编辑器。左栏只在最终页不可见时最小幅度滚动，让其进入视野，不把整张缩略图列表重置到顶部。

### 5.6 加载、清晰度和失败反馈

进入大图后先显示当前页的可用预览，再逐步替换为清晰渲染；短暂等待显示“正在加载清晰页面…”。切到另一页时立即更新目标页标识，并用加载态区分旧画面，不能把上一页误标为新页。

放大后应按视口、缩放和设备像素比提升渲染分辨率。扫描 PDF 的原始图像可能本来就模糊，重新渲染只能避免额外的显示模糊；不能承诺恢复原文件不存在的细节。

页面失败显示该页号、重试按钮，仍可翻到其他页；缺失文件与需要密码的 PDF 要分别说明。密码输入属于后续导入优化，本轮至少不能统一误报为“页面为空”。

## 6. P1：增加边看边写的阅读布局

工具区增加 `浏览布局 / 阅读布局` 切换。浏览布局适合找画面，阅读布局适合长时间读稿对照。两种布局共享当前页、Block 和未提交文案，切换不重新导入 PDF。

建议阅读布局分为三个区域：左侧可折叠页码缩略条，中间大图阅读区，右侧文案编辑区。素材与 Block 状态归入右侧可折叠区域，避免同时占用四个常驻栏。

以约 1440 CSS px 宽工作区为例，页码缩略条约 128 px，文案区约 400 px，中间剩余区域用于漫画；分隔线允许拖动。低于约 1200 CSS px 时先折叠缩略条，保留漫画与文案。这里的数值是待样机验证的初始值，不能由用户提供的左栏截图直接推导整个窗口布局。

漫画区默认适合宽度，编辑区保留当前及相邻 Block 上下文。支持“一键展开阅读”临时进入 P0 大图，再返回原布局。保存用户选定的布局和分栏宽度；超出当前窗口的旧尺寸须自动修正。

## 7. 配图与裁切：阅读器稳定后接入

先完成可靠的大图阅读，再开放阅读器中的配图操作。操作区应显示明确目标，例如 `加入 Block #018`，添加后提示“已加入 #018，第 3 张素材”，并提供撤销。

增加明确的 `阅读 / 框选` 工具切换：阅读模式左键拖动用于平移，框选模式左键拖动用于画框。两种模式使用不同光标，框选状态持续显示，避免用户想移动图片却画出选区。

框选确认保存相对当前 PDF 页的归一化坐标，同时保留 PDF 文件身份和物理页号；缩放与设备像素比不改变选区指向的内容。PDF 页面自身旋转应纳入统一坐标基准，本轮无需新增任意旋转工具。

Esc 优先取消当前未确认选区，再次 Esc 才关闭大图。切页或切 Block 时取消未确认框选并轻量提示，禁止把选区悄悄加入另一个 Block。

## 8. PDF 特有的信息与长文档体验

目录可能包含多个 PDF 和普通图片，因此左栏的“196 页”只能代表项目总页数。大图应显示 `01.pdf · 第 3 / 196 页` 这样的来源内页码；混合资源项目必要时另列“项目第 205 页”，避免与漫画印刷页码混淆。

页码输入跳转采用文件内物理页号，文件名搜索保留。用户需要区分“跳到第 128 页”和“搜索文件名含 128”的动作，不能依靠一个模糊搜索输入同时解释两者。

缩略图用于定位时建议保持整页比例，完整展示页面，而不是把所有页面填满固定方框；空白可以保留，选中边框继续使用橙色。缩略图密度调整属于 P1，不作为 P0 前置。

后续可增加连续纵向阅读；日漫双页与从右向左翻页按真实使用需求加入设置，默认不根据 PDF 文件名猜测阅读方向。P0 先统一单页浏览，控制学习成本。

## 9. 交互与可访问性约定

| 场景 | 键盘/鼠标行为 |
|---|---|
| 缩略图已选中且没有文本输入焦点 | Space 打开大图；显式“放大查看”按钮可由 Tab 到达 |
| 大图阅读区 | Space 或关闭按钮返回；←/→ 翻页；普通滚轮阅读；Ctrl + 滚轮缩放 |
| 大图工具栏按钮聚焦 | Enter/Space 执行该按钮，不被全局预览切换抢走 |
| 页码输入 | Enter 确认跳页，A/D 和数字作为正常输入 |
| 中文输入法组合中 | 不执行阅读或配图快捷键 |
| 框选中 | Enter 确认选区，Esc 取消；处理后阻止事件继续冒泡 |

大图打开后焦点限制在大图层，关闭后恢复；工具栏需要文字或可读名称，不能只给无说明图标。按钮建议至少 32 × 32 CSS px，焦点样式不能仅依赖细微颜色变化。选中页同时以边框与“当前页”语义标识，加载和失败消息可被辅助技术读取。

截图中的次要文字偏暗，存在可读性风险；本次没有测量对比度或操作屏幕阅读器，因此不做无障碍合规结论。验收时需要检查实际主题颜色、键盘路径和系统 125%/150% 缩放。

## 10. 当前代码发现与开发约束

以下为静态代码发现，需开发修复或运行复核；它们不等同于所有运行状态都已复现。

| 位置 | 当前实现 | 对本方案的影响 |
|---|---|---|
| `src/App.jsx`：`Browser` | 顶部固定 `h-44`，页面 contain 在横向区域 | 确认尺寸不足的结构原因；大图承担细节阅读 |
| `PageMedia` 与大图层 | 缩略图 scale=0.18、顶部 0.6、大图 1；大图层整体绑定点击关闭 | 需要动态清晰度及独立关闭行为 |
| 缩略图列表 | `images.map` 为过滤结果逐一创建页面组件 | 196 页可能同时安排大量渲染；需测量并增加可见区加载与任务优先级 |
| Crop 添加回调 | `Browser` 传入 crop，但父级 `onAdd` 只派发 sourceId | Crop 数据未传入添加命令，配图入口开放前修复 |
| Crop 定位 | 使用媒体元素盒尺寸计算及容器百分比画框，未排除 contain 留白 | 竖页留白下存在坐标偏差，应统一基于实际页面内容矩形计算 |
| Crop 控件 | 按钮标注 Enter/Esc，但全局监听没有对应 Crop 确认/取消分支 | 提示与实际操作不一致，需真实绑定并验证优先级 |
| `ProjectService.normalize` | 每个 Block 都被设置 `assets: []` | 保存/读取会清空素材；这是配图交付阻断项 |
| `Status` | 仍显示“素材 Phase 3”，未消费传入的素材/定位参数 | 添加后的可见反馈和素材列表尚未形成闭环 |
| 当前页恢复 | 启动按 path 匹配 `currentSourceId`，切页时仅 setSelected | 需要统一来源 ID 和阅读会话状态，才能可靠恢复页码 |

大图 P0 可独立交付；若包含“加入 Block”或裁切，必须先修复素材丢失及裁切传参问题，再验收重启恢复。

推荐把 PDF 阅读逻辑集中到可复用阅读器中，避免左栏、大图和阅读布局各自维护一套页码及裁切状态。渲染策略采用当前页优先、相邻页少量预取、缩略图可见区加载；快速翻页取消过时渲染，并对画布像素量和缓存设上限。具体缓存数量应通过测试确定。

PDF 文档实例复用可以保留；加载失败的缓存项需要允许重试，切换项目时释放不再使用的文档与画布。无需为本轮引入 OCR 或新的 PDF 解析框架。

## 11. 交付分批与优先级

| 批次 | 必须交付 | 成功标志 |
|---|---|---|
| P0：解决看不清 | 可见放大入口、应用内大图、适合整页/宽度、缩放平移、页内滚动与翻页、关闭及焦点恢复、清晰度适配、加载失败重试 | 用户无需记快捷键就能查看细节并接着写稿 |
| 配图前置修复 | 素材持久化、Crop 传参和坐标、素材结果展示、实际快捷键 | 用户添加的内容与看到的选区一致，重启不丢失 |
| P1：持续创作 | 阅读布局、分栏拖动与记忆、缩略条、文档内跳页区分、可靠配图 | 可长时间同时读漫画和写 Block |
| P2：按使用反馈选择 | 连续滚动、双页/阅读方向、双屏独立窗口 | 有明确频繁使用场景后再投入 |

P0 优先保证当前页响应；如果全部页面并发渲染已影响大图打开，虚拟列表或可见区懒加载须提前到 P0。原有主题、卡片和选中样式可复用，减少与任务无关的改版成本。

## 12. 验收用例与产品指标

以下均为拟定目标，没有被本次实测验证。性能测试需记录机器、应用窗口、系统缩放、PDF 大小及页面类型，区分首次加载和缓存命中。

| 用例 | 验收要求 |
|---|---|
| 首次用户查看 P003 | 不提示 Space，用户通过可见按钮一步进入大图 |
| 竖页对白细节 | 适合宽度或继续放大后能辨认原文件已有细节，画面不拉伸 |
| 鼠标缩放 | 围绕指针位置缩放，目标对白不发生明显漂移 |
| 平移与误关闭 | 点击/拖动页面不会退出；关闭按钮、Esc 正常工作 |
| 连续看 20 页 | 无需退出大图，页号与最终画面始终一致，无旧任务覆盖新页 |
| 返回写稿 | 当前 Block、文案内容、光标和编辑区位置保持；焦点回到合理入口 |
| 查看其他页后关闭 | 左栏选中最后查看页，只做必要滚动，搜索规则明确 |
| 页码异常 | 0、总页数+1、空值和文字均有就地反馈，不崩溃、不误添加素材 |
| 单页渲染失败 | 可重试或翻页，其他页面与文案仍可操作 |
| 196 页与 1000 页资源 | 可见页优先加载，快速翻页不持续堆积过时任务，离开项目后资源可释放 |
| 图片目录回归 | 普通图片使用同一大图入口和缩放操作，原有导入及编辑不退化 |
| 125%/150% 系统缩放 | 关闭、翻页和缩放控件可达，不出现工具栏覆盖核心内容 |
| 配图扩展验收 | 同 PDF 不同页及不同 Crop 保存后重启，来源、框选、顺序完整恢复 |

建议第一轮目标：打开大图后 100ms 内出现界面反馈；本地缓存命中时 500ms 内显示可用当前页；约定测试样本下首次清晰渲染 P95 ≤ 2 秒。超出时保持反馈和取消能力，不把目标写成对所有超大 PDF 的性能承诺。

邀请 3–5 位目标创作者完成“选页 → 看清一句对白 → 翻两页 → 返回继续写”任务，观察入口发现时间、误关闭次数、误添加次数、返回后重新定位次数。初轮期望至少 4/5 能在无快捷键教学下完成任务，误关闭与误添加为 0；小样本结果用于发现问题，不代表普遍统计结论。

## 13. 与已有开发文档的关系

本方案是 `phase2.md`、`phase3.md` 的阅读体验补充。继续沿用 NarrationBlock、延迟生成裁切图片、整页/裁切绑定和原图定位的产品方向。

本轮明确补充：Space 对应有完整控件的大图阅读器；双击缩略图仍加入 Block；默认阅读拖动用于平移，框选须进入明确工具模式；PDF 素材必须保留文件与页号身份。未来实施时应同步这些约定到 Phase 3 文档，避免出现两套快捷键与手势定义。

建议最先实施 P0 大图阅读器，用当前 196 页资源验证“放大查看—辨认细节—返回写稿”的完整流程，再决定阅读布局是否应成为默认布局。
