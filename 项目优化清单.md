# 项目优化清单

> 基于对仓库当前代码的扫描整理，覆盖**性能**、**使用体验**、**产品与工程**三个角度。
> 每条都标注了具体文件与行号，便于直接定位；优先级按「收益 ÷ 成本」排序。
> 配套文档：`项目架构说明.md`（目录职责与整体架构）。

---

## 0. 结论：最该先做的 5 件事

| # | 事项 | 影响 |
| --- | --- | --- |
| 1 | `pnpm test` 是**假绿**：脚本指向 `tests/*.test.cjs`，仓库里没有任何匹配文件，`node --test` 报 `tests 0 / pass 0` 且 **exit 0** | 目前等于没有质量门禁 |
| 2 | 每次按键都把整个项目快照推给主进程，并为**每个素材**算一次 SHA-256 | 最高频的 CPU + IPC 热点 |
| 3 | 导出时 PDF **每页都重新整文件读取并解析一次** | 大 PDF 导出极慢 |
| 4 | 素材列表无虚拟化、全项目零 `React.memo` | 大项目下每次按键重渲染整棵树 |
| 5 | 窗口宽度 < 1180px 时「当前段检查器」被 `display:none` 且没有替代入口 | 直接丢功能，无提示 |

---

## 1. 性能

### P1-1｜每按键的全量 IPC + 哈希（最高频热点）

`App.jsx:177-200` 的 effect 依赖 `[project, blocks, state.revision, state.saveStatus]`，而 `blocks` 在每次 `updateText` 后都会换新引用，于是**每敲一个字**都会：

1. 把整个 snapshot（所有 blocks + assets + sources）结构化克隆过 IPC 传给主进程；
2. `registerAssistantIpc.js:26` 对**每个 asset** 执行 `contentVersion()`，即 `sha256(JSON.stringify(asset 含整个 source 对象))`（`MediaDeliveryService.js:10-16`）；
3. 再把整包转发给助手窗口——**即使助手窗口没有打开**（`AssistantWindowService.send` 里才判断窗口是否存在）。

**修法**：`publish` 加 300–500ms 防抖；`contentVersion` 改为对稳定字段做浅哈希并缓存；助手窗口未打开时只存快照、不转发。

### P1-2｜导出 PDF 每页整文件重读

`AssetRenderer.js:93-105` 的 `renderPdfPage` 每次调用都 `fs.readFile(file)` 全量读入 + `getDocument` 解析 + `destroy`。
一本 300 页的 PDF 导出 = **300 次全文件读取与解析**，且 `disableWorker: true` 会在主进程同步阻塞事件循环。

**修法**：按 `pdfPath` 缓存 loadingTask，job 结束时统一 destroy。渲染进程的 `PdfDocumentRepository.js` 已经是这个模式，直接复用思路。

### P1-3｜大列表无虚拟化 + 零 memo

- `MangaBrowser.jsx:58-79` 直接 `sources.map()` 渲染全部来源，每张卡片一个 `PageMedia` + 一个 `IntersectionObserver`（`PageMedia.jsx:21-31`）。300 页 PDF = 300 个组件 + 300 个 observer（像素渲染是懒的，但组件与观察器全量创建）。
- 全项目**没有任何 `React.memo`**，而 `ProjectStoreProvider` 的 context value 包含整个 `state`，任何一次 dispatch 都重渲染整棵树（`MangaBrowser`、`BlockEditor`、`BlockStatusPanel`、`VideoWorkspace` 全在内）。
- `MangaBrowser.jsx:59` 在 `map` 里调 `allSources.indexOf(source)`，整体 O(n²)。

**修法**：给 `MangaBrowser` / `BlockEditor` / `BlockStatusPanel` 包 `React.memo`（props 已基本是稳定引用）；列表引入窗口化；`indexOf` 换成预建的 `Map`。

### P2-1｜导出管线

| 问题 | 位置 | 说明 |
| --- | --- | --- |
| 串行渲染 | `ExportService.js:127-167` | 逐 asset `await`。图片渲染（sharp/libvips 有线程池）可 4 路并发，视频保持串行 |
| 重复解码 | `AssetRenderer.js:51` | 每个 asset 都 `sharp(file).rotate().toBuffer()` 全量解码，同一源页被多个 Block 复用时重复工作；缺少像 `VideoFrameService` 那样的源级缓存 |
| 无法断点续做 | `ExportService.js` | 中途失败（磁盘满、来源变更）需全部重来 |

### P2-2｜大文件全程走 IPC 内存

| 问题 | 位置 |
| --- | --- |
| `readPdf` 返回整个 PDF 的 `Uint8Array` 过 IPC，渲染层 `getDocument({data})` 常驻内存 | `SourceScanService.js:44-47` |
| 校验时 `pdfPageCount` 又整读一遍 | `SourceScanService.js:73-78` |
| `readTake` 把整个 WAV 过 IPC（10 分钟录音约 57 MB） | `VoiceRecordingService.js:59` |
| 项目打开/最近项目每次都重扫全部来源目录（含重新整读 PDF） | `registerProjectIpc.js:8` |

项目里已有 `studio-video://` 自定义协议做 Range 流式传输（`VideoPlaybackService.js`）。PDF 和配音试听完全可以复用这套做法：改成 pdfjs range 加载 + 音频协议流式播放。

### P2-3｜reducer 的全量深拷贝

`projectReducer.js:18-31` 的 `contentUpdate` 对 **47 个 action 里的 35 个**执行 `structuredClone(整个 project)` + 全量 `normalizeProjectShape`（重扫所有 sources / blocks / assets）。`SET_BLOCK_STATUS`（勾一个进度框、助手窗口改状态）这种小改动也是 O(项目规模)。加上 undo 栈保留最多 100 份完整快照（`history.js`），内存随项目规模线性增长。

**建议**：改成结构化浅更新（只替换受影响的 block/asset）；`normalizeProject` 只在 LOAD 时做一次。

> 注：`updateText`（`projectReducer.js:42-65`）已经做了浅拷贝 + 700ms 历史合并，打字路径本身没问题，不要误伤。

### P3｜其他

| 问题 | 位置 | 建议 |
| --- | --- | --- |
| 主 bundle 单 chunk **792 KB**，pdfjs 被静态打进主 chunk | `PdfDocumentRepository.js:1` | `getDocument` 改动态 `import()`；`#/capcut` 走独立入口 |
| 录音电平每个音频回调（约 85ms）触发一次全局重渲染 | `VoiceRecorderPanel.jsx:133` | 电平值走 ref + 节流，或用局部组件隔离 |
| `project:open` / `project:recent` 每次重扫全部来源目录 | `registerProjectIpc.js:8` | 加扫描缓存，按目录 mtime 失效 |

---

## 2. 使用体验

| # | 问题 | 位置 |
| --- | --- | --- |
| 1 | 窗口宽 < 1180px 时第三栏（检查器 / 素材篮 / 撤销按钮）被 `display:none`，**无任何替代入口**，而窗口 minWidth 是 1040 | `src/index.css`、`BlockStatusPanel.jsx:37` |
| 2 | 视频导入对话框列出 `mkv` / `webm`，但探测只接受 h264 + yuv420p + mp4/m4v/mov，选了一定失败（提示「兼容预览副本尚未开放」） | `registerVideoIpc.js:13` vs `VideoProbeService.js:20` |
| 3 | 录音硬性要求 48 kHz，设备不支持直接抛「当前设备无法提供 48 kHz 录音」；应允许设备原生采样率 + 重采样 | `VoiceRecorderPanel.jsx:104-108` |
| 4 | 使用已废弃的 `createScriptProcessor`（主线程），应换 `AudioWorklet` | `VoiceRecorderPanel.jsx:122` |
| 5 | 保存失败后自动保存**停止重排**，界面上只有一个小圆点 + 一行文字，没有「重试」按钮，用户容易以为仍在自动保存 | `useProjectAutosave.js:37`、`AppHeader.jsx:22` |
| 6 | 编辑文案会把 `scriptDone` **静默置回 false**——改个错别字就丢掉「文案完成」标记，无提示 | `projectReducer.js:53` |
| 7 | 没有快捷键帮助面板；`Ctrl+S` / `Ctrl+Enter` / `B` / `Q` / `Enter` / `Delete` / 空格 只散落在个别 `title` 里 | `App.jsx:220-231` |
| 8 | 来源文件丢失后只报「请恢复原文件」，而 `relocateSources` 能力**已有但无 UI 入口** | `registerProjectIpc.js:27`、`ProjectCenter.jsx` |
| 9 | 项目中心只有「归档」：没有重命名（`renameProject` 未接线）、没有删除、看不到已归档列表、卡片无缩略图 | `ProjectCenter.jsx` |
| 10 | 来源可用性只在 Storyboard 视图校验，工作区不提示 | `StoryboardView.jsx:22` |
| 11 | 自定义下拉菜单是 div 实现，无键盘方向键导航 / 焦点管理 | `AppHeader.jsx:29-30` |

---

## 3. 产品与工程

1. **`pnpm test` 假绿**（见结论第 1 条）。建议优先补 `src/**/model/`、`src/store/`、`electron/services/` 的纯函数单测——`crop.js`、`videoRange.js`、`usageIndex.js`、`validateStoryboard.js`、`ExportPlanner.js`、`projectNormalize.js` 都是零依赖、易测的。
2. **`dist/` 被 git 跟踪**：4 个文件在索引里，且**当前工作区就是脏的**（旧构建已删、新构建未跟踪）。与 `AGENTS.md` 的「不要提交 dist」冲突。建议加进 `.gitignore` 并 `git rm --cached`。
3. **无 ESLint / Prettier / EditorConfig / CI**：`src/` 用 4 空格、`electron/` 用 2 空格，全靠人工维持。
4. **双份来源真值**：`App.jsx` 的 `sources` 本地 state 与 `project.sources` 并存，靠 `sourcesById` 手工合并（`App.jsx:42`），新增来源、重开项目都要手动同步，容易出现不一致。
5. **未接线的 API 面**：`project:rename`、`project:relocate-sources`、`system:copy-text`（`copyText` 在渲染层从未被调用，助手走的是 `assistant:copy`）。要么接 UI，要么删掉，避免误以为已支持。
6. **助手素材缓存无限增长**：交付版本被设计成永久保留（`MediaDeliveryService.js:102-103`），但用户没有查看 / 清理入口。
7. **schema 演进只有「拒绝降级保存」**，没有迁移用例或回归样本（`projectNormalize.js:4`），跨版本升级风险靠人工验证。
8. **全部文案硬编码中文**，无 i18n 层。若只面向中文用户可接受，但需在产品上确认。

---

## 4. 建议的落地顺序

| 阶段 | 内容 | 预期收益 |
| --- | --- | --- |
| 第 1 周 | 修 `pnpm test` 假绿 + 补 6~8 个纯函数单测；`dist` 移出版本控制；加 ESLint / Prettier | 恢复质量门禁，止住噪声 |
| 第 2 周 | `assistant:publish` 防抖 + 哈希缓存；三处 `React.memo`；`MangaBrowser` 窗口化 | 打字与大项目滚动明显跟手 |
| 第 3 周 | PDF 文档缓存复用；导出图片并行 + 源解码缓存 | 大 PDF 导出提速最明显 |
| 第 4 周 | 窄窗口检查器替代入口；保存失败重试；视频过滤器与探测对齐；录音采样率兼容 | 直接减少用户卡壳 |
| 后续 | PDF / 音频改自定义协议流式；`relocateSources` UI；项目中心补重命名 / 删除 | 内存与产品完整度 |

---

## 5. 附：逐项核对表

- [x] `tests/*.test.cjs` 存在真实用例，`pnpm test` 在无用例时退出码非 0
- [x] `dist/` 加入 `.gitignore` 并从索引移除
- [x] ESLint + Prettier + EditorConfig 落地，CI 跑 lint + test
- [x] `assistant:publish` 防抖，助手窗口关闭时不转发
- [x] `contentVersion` 哈希缓存
- [x] `AssetRenderer` 复用 PDF 文档与源解码结果
- [x] 导出图片并行、视频串行
- [x] `MangaBrowser` / `BlockEditor` / `BlockStatusPanel` 加 `React.memo`
- [x] 素材列表窗口化，`indexOf` 换 `Map`
- [x] 1180px 以下提供检查器替代入口
- [x] 保存失败提供显式重试
- [x] 视频导入过滤器与探测能力对齐（或实现转码）
- [x] 录音采样率兼容 + `AudioWorklet`
- [x] `relocateSources` / `renameProject` 接 UI 或删除
- [x] 配音试听改协议流式
- [ ] PDF 阅读改 range 加载（pdf.js 不支持自定义协议，见 6.5）
- [x] 助手素材缓存提供查看 / 清理入口

---

## 6. 本轮完成情况

### 6.1 工程基建

| 变更 | 说明 |
| --- | --- |
| `dist/` 移出版本控制 | `.gitignore` 增加 `dist/`，并 `git rm -r --cached dist`（文件保留在磁盘） |
| ESLint 9（flat config） | 新增 `eslint.config.js`：渲染层按 ESM + JSX + browser 全局，主进程/preload/scripts/tests 及 `src/shared/domain/*.js` 按 CommonJS + node 全局；开启 `react/jsx-key`、`react-hooks/rules-of-hooks`（error）与 `exhaustive-deps`（warn） |
| Prettier + EditorConfig | 新增 `.prettierrc.json`、`.prettierignore`、`.editorconfig`，与现有风格对齐（单引号、无分号、`src/` 4 空格、`electron/` 2 空格），格式化按文件可选执行 |
| CI | 新增 `.github/workflows/ci.yml`：`pnpm install --frozen-lockfile` → `lint` → `test` → `build` |
| 新增脚本 | `lint`、`lint:fix`、`format`、`format:check` |
| 测试 | 新增 `tests/resample.test.cjs`、`tests/localMediaService.test.cjs`、`tests/mediaDeliveryCache.test.cjs`、`tests/projectRelocate.test.cjs`，合计 18 个用例全部通过 |

`pnpm lint` 当前 **0 error / 10 warning**（warning 均为既有的 hook 依赖提示，属可读信号，不阻断 CI）。

### 6.2 性能

| 变更 | 文件 |
| --- | --- |
| 图片源按文件缓存解码结果（指纹校验 + 字节预算 LRU + 并发去重），PDF 页面按 `文件#页@DPI` 缓存光栅结果；导出/交付任务结束后统一释放 | `electron/services/AssetRenderer.js`、`ExportService.js`、`MediaDeliveryService.js` |
| 导出改为「图片并发 4 路、视频严格串行」，manifest 顺序不变，取消语义保留 | `electron/services/ExportService.js` |
| 素材列表按行窗口化（固定卡片高度 164px，行距 176px，上下留白补齐滚动高度），选中项若在窗口外先滚动定位；`allSources.indexOf` 已改为 Map 查表 | `src/features/library/components/MangaBrowser.jsx`、`src/components/ui/scroll-area.jsx` |
| 配音试听改协议流式：`<audio>` 直接播放 `studio-media://audio/<token>`，长录音不再整段拷贝 | `registerVoiceIpc.js`、`VoiceRecordingService.resolveTake`、`VoiceRecorderPanel.jsx` |
| 顺带移除已无调用方的 `voice:read-take` 通道，避免整文件 IPC 残留 | `preload.js`、`mangaDeskBridge.js`、`registerVoiceIpc.js`、`VoiceRecordingService.js` |

构建产物变化：主 chunk 由 **792 KB 降至 390 KB**，PDF.js 拆分为按需加载的 `pdf-*.js`（421 KB）。

### 6.3 使用体验

| 变更 | 文件 |
| --- | --- |
| 录音采样率兼容：优先申请 48 kHz 上下文，失败或设备不支持时按原生采样率采集并在渲染层实时线性重采样回 48 kHz，落盘格式不变 | `src/features/voice/model/resample.js`、`VoiceRecorderPanel.jsx` |
| 录音采集改用 `AudioWorklet`（4096 采样一块回传，不占用主线程），`createScriptProcessor` 仅作为不支持时的兜底 | `src/features/voice/worklet/pcmCapture.worklet.js`、`VoiceRecorderPanel.jsx` |
| 来源丢失可就地修复：Storyboard 的问题标签旁新增「重新定位」，支持选文件或整个文件夹，PDF 页保留页码并自动登记新目录 | `StoryboardView.jsx`、`App.jsx`、`registerProjectIpc.js`、`ProjectService.relocateSource` |
| 项目重命名改为内联编辑（`window.prompt` 在 Electron 中不可用，原实现会直接报错），保留归档/恢复切换 | `ProjectCenter.jsx` |
| 助手窗口新增「素材缓存」面板：查看每份交付文件的大小与时间、显式删除单份交付、一键清理中断残留的临时目录 | `CapCutAssistant.jsx`、`MediaDeliveryService.stats/remove/pruneTemporary`、`registerAssistantIpc.js` |

### 6.4 仍待处理（未纳入本轮）

以下来自第 1~3 章但不在核对表内，建议后续单独排期：

- `projectReducer` 的 `contentUpdate` 仍对约 35 个 action 做整项目深拷贝 + 全量归一化（P2-3）。
- 录音电平 `setLevel` 每个音频回调触发一次全树重渲染（P3）。
- `App.jsx` 的 `sources` 本地 state 与 `project.sources` 仍是双份真值（3.4）。
- `system:copy-text` / `copyText` 仍无调用方（3.5）。
- schema 迁移缺少回归样本（3.7）。
- 全部文案仍硬编码中文，无 i18n 层（3.8）。

### 6.5 关于「PDF 阅读改 range 加载」的结论（重要）

**这项无法用自定义协议实现，已回退到原来的整文件 IPC 方式。** 依据是 `pdfjs-dist` 的源码（`node_modules/pdfjs-dist/build/pdf.mjs`）：

```js
// 只对 http(s) 使用 Fetch 流；其他协议一律走 XHR
function isValidFetchUrl(url, baseUrl) { return /https?:/.test(URL.parse(url, baseUrl)?.protocol ?? "") }
function getNetworkStream(url) { return isValidFetchUrl(url) ? PDFFetchStream : isNodeJS ? PDFNodeStream : PDFNetworkStream }

// 而且只有 isHttp 时才真的带上 Range 头
this.isHttp = /https?:/.test(url.protocol)
if (this.isHttp && "begin" in args && "end" in args) xhr.setRequestHeader("Range", `bytes=${args.begin}-${args.end - 1}`)
```

也就是说：给 PDF.js 一个 `studio-media://` 地址，它会退化成 **不带 Range 的 XHR 全量请求**，既拿不到 range 流式收益，还会因 `file://` 源跨域而被拦。因此 `pdf:read`（整文件 IPC）保留，`studio-media://` 只用于 `<audio>` / `<video>` 这类由浏览器原生做 Range 的媒体元素。

如果后续仍要做 PDF 流式，只有两条可行路径，且都必须在真实 Electron 环境里验证：

1. **回环 HTTP 服务**：主进程起一个只监听 `127.0.0.1` 的随机端口 HTTP 服务，按 token 提供已登记的文件并支持 Range；PDF.js 拿到 `http://127.0.0.1:PORT/pdf/<token>` 后才会走 Fetch + Range 分支。
2. **`PDFDataRangeTransport`**：把字节范围请求改走 IPC 分片（`getDocument({range: transport})`），需要验证初始读与 xref 读取的时序。

> 注：`pnpm test` 在本机沙箱内会因 Node 测试运行器为每个测试文件派生子进程而报 `spawn EPERM`；在沙箱外（含 CI）按原脚本执行即可，验证时可用 `node --test --test-isolation=none tests/*.test.cjs` 在同进程内运行。

