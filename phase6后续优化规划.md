# MangaDesk Phase 6 后续优化与效率提升规划

> 日期：2026-09-07  
> 依据：`init.md`、`phase2.md` 至 `phase6.md`、`voice-narration.md`、阅读体验优化方案及当前代码  
> 定位：Phase 6 完成后的产品演进建议，用于选择下一阶段工作，不表示下列功能已经实现或全部需要实现。

## 1. 产品判断

Phase 6 完成后，MangaDesk 将具备“导入 → 阅读 → 文案/录音 → 配图 → 检查 → 导出/剪映辅助”的基本闭环。后续最值得投入的方向，是减少重复定位、重复输入和改稿返工，并让用户放心保存、恢复和迁移自己的创作成果。

建议先把“一期作品做完”优化成“每天打开就能接着做，多期作品都能管理，改过的内容不会漏交付”，再考虑更复杂的画面工具。继续遵守 `init.md` 的原则：判断由人完成，机械操作由软件完成。

本文来自工作流分析及静态代码检查，尚未进行用户访谈、运行中的界面审计或效率实测。优先级、开发规模和验收目标均为建议，实施前应对照 Phase 6 最终代码确认差距。

## 2. 已有基础与真正需要补足的部分

| 已有或已规划能力 | 后续改进应解决什么 |
|---|---|
| Block 创建、排序、复制、撤销 | 整篇稿件导入、拆分合并、多选整理 |
| 收藏、素材篮、Used 标记 | 长项目快速定位、章节筛选、重复画面对照 |
| Storyboard、缺项检查、完成状态 | 下一待办导航、改动影响提示、连续审阅 |
| 自动保存与项目 `.bak` | 可发现的版本恢复、完整备份、历史项目入口 |
| 真人录音、多 Take、裁剪及导出 | 连续录制效率、提词、前后段试听，而非重复开发录音 |
| Phase 5 素材包导出 | 导出预设、版本对比、明确的差异交付 |
| Phase 6 辅助窗口、拖出和缓存 | 从上次制作位置继续、录音素材交接及修订提示 |

当前 `useProjectImport` 在导入来源时调用 `createProject`，因此“新建项目”和“追加来源到当前项目”值得明确分开。当前 Welcome 以导入为入口，ProjectService 记录单个最近项目；虽然已有 `.bak` 写入机制，但它不等于用户可选择的历史版本或包含全部素材的备份。这些是当前代码基线，不应被描述成 Phase 6 完成后的确定缺陷。

## 3. 从用户任务看效率损失

| 用户任务 | 可能的重复劳动 | 建议改进 | 优先级 |
|---|---|---|---|
| 第二天继续做昨天的作品 | 找项目、找页、找段、回忆下一步 | 项目中心与继续创作 | P0 |
| 一本漫画做到后续章节 | 为导入和来源顺序反复整理文件 | 追加导入、章节组织、自然排序 | P0 |
| 已在别处写好解说稿 | 一段段复制并创建 Block | 文案批量导入和拆分预览 | P1 |
| 读图时写稿或配音 | 大图和编辑区来回切换 | 可调整阅读布局与焦点恢复 | P1 |
| 修改段落结构 | 新建、复制、删字、重新配图 | 光标拆段、合并段落、多选操作 | P1 |
| 检查一整期 | 逐段寻找缺图或待录音的位置 | 待办队列与下一问题导航 | P1 |
| 已交付后改稿 | 回忆哪些文字、图片和录音变过 | 修订提示与交付版本对比 | P1/P2 |
| 原文件移动或换电脑 | 重新导入、重新绑定、担心丢稿 | 来源重定位、备份与迁移 | P0 |
| 长时间录制旁白 | 反复点按钮、找提词和试听位置 | 连续录音助手与上下文试听 | P2 |

P0 表示下一轮优先解决的基础问题；P1 表示建立在可靠基础上的高频效率提升；P2 表示需要实际使用反馈验证的增强项，不是所有功能必须依次全部完成。

## 4. P0：项目中心与继续创作

### 4.1 项目列表

首页增加“最近项目 / 全部项目 / 新建项目 / 打开项目”。卡片显示名称、最后编辑时间、封面、Block 数和当前制作进度，支持搜索、重命名和归档。归档只是从活跃列表隐藏，不删除项目或素材。

项目多了之后，创作者应该能直接打开“第 12 期”，而不是再次导入漫画。首次使用仍保持简单导入入口，无需先填写复杂项目属性。

### 4.2 恢复工作现场

“继续创作”恢复上次 Block、来源页、阅读位置、缩放、布局及当前工作模式。对文案编辑区，在同一会话视图切换时恢复光标和选区；跨重启恢复到最近段落和合理滚动位置即可，不承诺恢复已变化文本中的精确光标。

主工作区位置和剪映辅助位置分别记录，避免为了查看一张原图改变制作进度。退出前未结束的录音沿用现有恢复流程，不能因为恢复工作现场而自动开启麦克风。

验收：连续关闭和打开同一项目三次，均可从上次段落与页面继续；归档再恢复不丢失关联文件；切换项目前处理未保存修改及进行中的任务。

## 5. P0：多章节追加导入与来源管理

### 5.1 明确导入意图

在已有项目中提供“添加漫画来源”，新项目入口单独命名。支持向当前项目追加图片目录或多个 PDF，并在确认前展示文件、页数、章节分组、重复来源和预期顺序。

批量选择来源后可以统一导入，按文件名自然排序，例如 `2.jpg` 排在 `10.jpg` 前；同时允许用户手动调整章节顺序。物理页码、章节内位置与项目总页码分别呈现。

### 5.2 稳定来源身份

追加来源保留原有 sourceId，不重建已有 Block、Crop、收藏或素材篮引用。相同文件重复选择时提示已有来源；不同目录的同名文件不能直接判定为重复。内容去重可后续增强，不以全库哈希阻塞首次导入。

移除来源前展示受影响的 Block、收藏和候选画面。移除项目引用与删除磁盘原文件必须区分，默认不删除原文件。

验收：向已有 50 个 Block 的项目追加两章后，原素材引用及裁切完全保留；重复导入可识别；调整章节顺序不改变各素材身份。

## 6. P0：来源重定位、版本恢复与完整备份

### 6.1 找回移动的原文件

检查发现来源不可用时，提供“定位新文件”或“选择新的根目录”。先展示候选映射及受影响引用，用户确认后批量更新路径，保留 sourceId。

文件名只能作为候选依据。使用相对路径、尺寸、PDF 页数等辅助匹配，必要时检查内容哈希；多候选和不匹配项必须人工选择。对于内容已变或页面插入的 PDF，不得仅因同名就默认原物理页仍然正确，相关配图应进入待复核。

### 6.2 可浏览的恢复点

在已有原子保存与 `.bak` 基础上增加可见恢复入口。建议保留最近若干时间点，以及导入、批量改稿和导出前的恢复点；数量及空间预算可配置，自动保存不必每次都创建永久版本。

恢复前显示项目时间、Block 数和文案/素材变化摘要，并先保留当前版本。历史版本引用的录音 Take 必须受到保留策略保护，不能只恢复 JSON 后得到失效音频路径。重做历史恢复与普通 Undo 的边界应在界面中说明。

### 6.3 区分三种产物

| 产物 | 用途 | 是否包含原始来源 |
|---|---|---|
| 项目恢复点 | 撤回项目修改 | 通常不包含；不等于备份 |
| 剪映素材包 | 外部剪辑和审稿 | 按导出选项生成的素材，不是全部源文件 |
| 完整备份包 | 换机器、离线归档、灾难恢复 | 包含项目、原图/PDF、项目音频和必要清单 |

完整备份前展示大小和缺失文件，明确是否包含历史 Take、恢复点及 Phase 6 已交付文件。提供默认完整配置与可选精简配置；缺失内容必须列明，不能标为完整成功。备份在独立目录生成并校验，失败不污染已有包。

迁移恢复应在新目录完成，不覆盖当前项目。即使复制了 Phase 6 交付文件，外部剪映工程仍可能记录旧绝对路径，不能承诺自动修复剪映链接；应提示用户在剪映中重新定位。

验收：将完整备份恢复到另一目录后，在旧来源目录不可访问的条件下仍能阅读、配图、试听和导出；未知的新 schema 版本不得被旧程序破坏性保存。

## 7. P1：整篇文案导入、拆分合并与批量操作

### 7.1 从已有文稿开始

增加“粘贴全文 / 导入 TXT”，支持按空行或用户明确指定的分隔符拆成 Block。导入前展示拆分结果、数量及空段，允许手动调整，确认后一次性加入项目；默认追加，也可指定插入位置。

不自动润色、不根据句号强行拆句、不默认替换整个项目。存在 `【001】` 等编号时，仅在用户选择去除编号规则后处理，原文换行和中文标点保持稳定。导入一次对应一条可撤销命令。

### 7.2 光标拆段与合并

用户在正文光标处执行“拆成两段”，立即预览两段内容。默认素材保留在原段，新段无素材，并提供手动分配入口；不能猜测每张图对应哪句话。

“与下一段合并”保留正文顺序，素材按两段顺序拼接，重复引用不静默去重。受影响的人工完成状态和交付状态标记待复核。

存在录音时，文字边界不等于音频边界：V1 可禁止直接拆并已有录音段并解释原因；后续再提供明确的时间点切分或 Take 选择。不能因为改文字就自动切坏录音、删除旧 Take 或重复播放整段音频。

### 7.3 多选与搜索

支持连续或非连续多选 Block 后移动、复制、标记待办及加入审阅队列；删除显示影响数量并可整体撤销。增加文案全文搜索，结果展示上下文并跳转具体段落。

批量替换适合统一人物称呼，必须先预览命中位置、限定范围并确认；替换一次可撤销。修改后标记相关已交付段落需要复核，不自动修改录音内容。

验收：导入 100 段文稿后顺序与正文一致，Undo 一步恢复；拆并后素材和录音不丢失；批量操作不改变未选中段落。

## 8. P1：专注布局、导航与下一待办

阅读布局已有独立规划，后续应先检查哪些已经实现，再补剩余差距。提供可调整的漫画/文案分栏、收起次要面板、布局记忆；宽度不足时保留当前任务所需控件，不通过增加更多常驻面板挤压漫画。

轻量命令面板集中提供“跳到 Block / 跳到来源页 / 下一未配图 / 下一待录音 / 回到上次位置”。全文搜索和页码跳转明确区分，避免输入一个数字产生不确定结果。快捷键允许查看和有限自定义，并检测冲突。

“下一待办”按当前模式筛选：写稿、配图、录音、剪映制作。它跳过已完成项，不自动完成当前项，也不把创作者带入强制线性流程。

真人录音模式中，无参考文字不是缺稿；明确设为无旁白的段落不是缺录音。待办、检查和进度统一使用同一套模式规则。

验收：在 200 段中定位下一待配图段最多两次操作；修复问题返回列表时保留筛选和位置；中文输入、读图快捷键和剪映焦点互不干扰。

## 9. P1：修改影响提示与交付版本记录

### 9.1 让用户知道哪些成果需要重看

仅有“已完成”勾选无法回答“完成后有没有改过”。增加与人工状态分离的待复核提示：记录用户确认制作时的相关内容版本，与当前版本比较。

| 变化 | 提醒内容 | 不自动执行 |
|---|---|---|
| 正文变化 | 已制作的配音或字幕可能需要复核 | 不重写音频、不自动认定重录 |
| Crop、素材顺序或来源变化 | 已放画面可能与当前配图不同 | 不覆盖旧交付文件 |
| active Take 或 trim 变化 | 旁白与下游时间位置可能变化 | 不自动修复剪映时间轴 |
| Block 顺序变化 | 原交付顺序可能已过时 | 不重命名外部正在引用的文件 |

提示只说明客观变化及其可能影响，不判断叙事质量。用户可以“已检查，无需修改”或“需要重新制作”。Undo 回到原内容后，复核结果按内容版本重新计算，不能只因发生过一次编辑就永久显示变化。

### 9.2 区分导出、拖出和实际使用

成功导出素材包可以建立交付版本记录；启动一次拖动不代表剪映导入成功。Phase 6 的拖动记录只能称为“已准备/已发起拖出”，实际采用哪版由用户确认。

导出版本保存项目修订号、稳定 Block/asset ID、内容指纹、输出参数和清单。用户选择任意已保留版本与当前项目比较，看到“正文改动 3 段、画面改动 2 段、录音改动 1 段”。

验收：改一个 Crop 只影响相关画面提示；重新排序不会误判为全部新素材；修改正文不会删除已经录好的音频。

## 10. P2：差异交付、导出预设与录音交接

### 10.1 一次设置，多次使用

保存用户命名的导出预设，例如画布尺寸、背景、图片格式和旁白选项。预设保存参数，不绑定某一期素材或上次输出目录；导出前仍展示当前模式下的实际产物，语音模式保持不生成自动字幕的既有规则。

### 10.2 差异包

在交付版本对比可靠后增加“仅导出变更项”，输出到新的独立目录，附带新增、修改、移除及顺序变化清单。保留全量导出作为默认可靠路径；差异包不宣称可以直接合并成完整包，也不自动覆盖旧文件或替换剪映内容。

真人旁白的前段时长变化可能使所有后续起止时间移动。差异判断不仅比较音频文件，还需检查时间映射：合并旁白变化时应重新生成整条音频和 timing，不能只交一个局部片段却沿用旧总时间。

### 10.3 辅助窗口中的录音交接

在 Phase 6 图片拖出稳定后，允许从辅助窗口试听和准备当前 Block 的有效录音片段，再拖入剪映。应用 trim 后生成稳定 WAV，沿用交付文件不自动删除、不覆盖的规则。

清楚区分“整条旁白”和“当前段片段”，避免用户同时导入产生重复声音。此功能适合局部补录替换，并不等同于自动插入到正确时间点。

验收：同一预设重复导出参数一致；差异包能追溯到基线版本；修改首段旁白时正确指出后续时间偏移。

## 11. P2：连续录音与带声音的故事板审阅

### 11.1 减少录音按钮操作

在现有逐段录音上增加“保存并准备下一段”、可调字号提词、可选倒计时和重录快捷键。进入下一段后默认处于准备状态；只有用户明确启用连续录制会话时，才按可见倒计时开始下一次录音，暂停和退出入口始终可见。

支持先听前一段结尾再录当前段，帮助用户自行判断语气是否连贯。试听时关闭或隔离采集，防止播放声音被误录入；不自动选择最佳 Take，也不自动覆盖原录音。

### 11.2 在故事板中连续审阅

使用真实 active Take 和 trim 按 Block 顺序播放，突出当前段的配图，允许暂停、跳回修改和标记“需重录/需换图”。先以整段配图列表跟随音频，不默认推断每张图应出现的准确时刻。

文字模式可显示用户可调语速下的粗略时长，但必须标注估算；不将其当作真实配音时间、字幕时间轴或成片时长。没有音频也可以手动逐段审阅。

验收：试听与导出应用同一 trim 和段间隔；关闭审阅即停止播放；修改后返回原段，未保存数据不丢失。

## 12. P2：高频画面处理增强

有真实频繁需求后再选择以下能力，不建议一次全部开发：

| 能力 | 用户收益 | 最小可交付范围 |
|---|---|---|
| Crop 原位微调 | 已配好的图无需删除后重加 | 拖动边界、保持 assetId、Undo、原页定位 |
| 连续框选 | 同一页取多个画面减少重复切工具 | 每次确认后继续框选，目标 Block 持续可见 |
| 比例辅助线 | 更快选择适合输出的区域 | 自由/常用比例，用户自行判断构图 |
| 前后段画面对照 | 找出重复画面或衔接问题 | 并排看当前及相邻段，不自动评分 |
| 阅读方向与双页 | 适配不同漫画阅读习惯 | 用户明确选择，保持物理页身份与 Crop 坐标 |

图片旋转、镜像、遮罩和局部模糊会扩大非破坏性编辑模型及导出范围，优先级低于上述能力。若引入，应保存变换参数，统一预览与导出，不直接改写原图。

## 13. 支撑效率的技术改进

技术任务应绑定用户能感知的结果，避免以重构规模作为完成标准。

| 技术改进 | 用户结果 | 验证方式 |
|---|---|---|
| 阅读、缩略图、导出共用受限任务调度 | 导出期间仍能流畅翻页、写稿 | 混合 PDF 项目并行操作测试 |
| 可见区渲染与缓存预算 | 长项目不越用越卡 | 长时间操作后的内存与响应记录 |
| 统一领域校验及状态规则 | 工作区、故事板、辅助窗口提示一致 | 同一问题跨入口结果相同 |
| 文件任务原子发布与归属清单 | 中断后易恢复、不损坏旧成果 | 磁盘满、权限失败和中断注入 |
| schema 顺序迁移及历史样本测试 | 升级后旧项目仍可编辑 | 真实旧项目保存往返验证 |
| 本地诊断摘要及错误定位 | 出错后用户知道如何继续 | 错误包含任务、对象及可行操作 |

诊断默认保存在本机；分享前允许用户查看并去除路径、文案等内容，不为这些功能引入默认云上传。构建与自动化测试覆盖高风险文件操作、录音恢复、项目迁移和跨窗口一致性；纯样式改动无需堆积镜像实现的测试。

## 14. 建议分期

| 阶段 | 主题 | 必须完成 | 可后移 |
|---|---|---|---|
| Phase 7 | 可靠地持续创作 | 项目中心、现场恢复、追加来源、来源重定位、可见恢复入口 | 完整备份的高级配置 |
| Phase 8 | 批量创作和快速检查 | 文稿导入、基础拆并、全文搜索、下一待办、阅读布局补齐 | 多选高级操作、快捷键自定义 |
| Phase 9 | 改稿与交付追踪 | 待复核提示、交付版本记录、导出预设 | 差异包、录音片段拖出 |
| Phase 10 | 录音和画面效率增强 | 根据实际使用选择连续录音、声音故事板或 Crop 微调 | 双页、复杂画面变换 |

完整备份建议在 Phase 7 基础路径及依赖关系稳定后优先补齐，不拖到功能扩张结束。差异包必须后于可靠的交付清单与内容版本识别；跨窗口修改统一走主项目命令层；涉及录音段落拆并的增强必须后于明确的音频边界模型。

不建议一次进入四个阶段。先交付一个可以独立减少实际操作的闭环，再根据使用数据选下一项。

## 15. 如何判断是否真的提高效率

以同一用户、相近作品规模和相同机器比较改进前后，区分初次学习与熟练使用。可先邀请 3–5 位创作者观察问题，小样本用于发现障碍，不代表普遍统计结论。

| 任务 | 记录指标 | 初始验收目标 |
|---|---|---|
| 隔天继续创作 | 从打开应用到可继续编辑的操作数 | 选项目后一次“继续创作”，无需重新找页找段 |
| 追加一章漫画 | 重新绑定次数、顺序修正次数 | 已有绑定无需重建 |
| 导入 100 段已有文稿 | 手动创建段落次数、正文错误数 | 一次预览确认，无逐段复制，正文无损 |
| 修复 10 个缺项 | 找问题与回到原位置的次数 | 可沿待办队列逐项处理 |
| 修改 3 段后再交付 | 漏交改动数、错误覆盖旧文件数 | 变化清单完整，旧交付文件不受影响 |
| 从备份恢复 | 缺失来源、录音不可用、不可导出次数 | 完整备份恢复后编辑与导出均可用 |
| 连续录制 20 段 | 每段操作数、误录次数、重录定位时间 | 操作数低于原流程，误录和丢 Take 为 0 |

耗时目标先建立基线再设定，不预先承诺“效率提升 50%”。应优先观察减少了多少次复制、切窗、重新定位和重复渲染，以及有没有引入更难发现的错误。

## 16. 暂不建议优先投入

暂不优先开发 AI 写稿/选图、OCR 全库索引、自动拆格、自动视频或剪映工程、云账号和多人协作，也不以更换技术栈作为下一阶段目标。这些方向可能有价值，但尚无证据说明它们比项目恢复、批量整理和修订交付更能解决当前用户的高频问题，并且部分超出 `init.md` 的既定边界。

若只选择三项先做，建议是：项目中心与继续创作、多章节追加导入和来源重定位、整篇文稿导入与基础拆并。与此同时，把可见恢复入口作为基础保障；待完整作品真实使用后，再决定优先加强录音、审阅还是差异交付。
