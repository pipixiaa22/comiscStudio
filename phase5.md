# MangaDesk Phase 5 开发文档

> 主题：素材包导出、顺序命名、script.txt 与离线 storyboard.html  
> 依据：`init.md` 第 7–8、31–35、43–45 节  
> 前置条件：Phase 3 配图闭环和 Phase 4 素材管理、Storyboard 已完成  
> 阶段目标：用户一次导出按剧情排序的实际图片、文案和离线故事板，供剪映手动导入与审稿留档。

## 1. 交付范围

本阶段交付统一“导出素材包”入口、导出前检查、输出格式选择、图片实体生成、稳定命名、文案和 HTML 生成、项目快照、任务进度/取消/失败恢复，以及完成后打开目录。

不生成剪映工程、视频、配音、字幕时间轴或自动关键帧。Native File Drag、置顶剪映辅助窗口、拖动时临时渲染属于 Phase 6。PDF 支持沿用项目现有来源能力，导出的是被 Block 引用的具体页面或选区，不是把整个 PDF 转成素材包。

## 2. 输出结构与语义

```text
Episode01/
├── images/
│   ├── 001_01.jpg
│   ├── 001_02.jpg
│   ├── 002_01.jpg
│   └── 003_01.jpg
├── script.txt
├── storyboard.html
└── project.json
```

`images/` 仅包含 Block 实际引用的整页或 Crop；收藏和素材篮不生成图片。一个来源引用多次，按每次使用生成独立命名的导出文件，保证顺序和人工导入习惯；可以复用内部渲染结果降低计算量。

`project.json` 是完整项目快照并附带导出映射，用于追溯 Block、来源与输出文件。它不代表完整可迁移编辑工程：原图片/PDF 没有随包复制，换机器继续编辑仍需要原始来源。`storyboard.html` 和 images 使用相对路径，可随整个目录离线移动查看。

此区分必须在导出完成说明中可见，避免用户误把素材包当作包含全部原文件的项目备份。

## 3. 用户流程

1. 在工作区或 Storyboard 点击“导出素材包”。
2. 选择目标父目录，输入素材包名称，选择图片格式和尺寸策略。
3. 查看导出预检：Block 数、素材数、警告及阻断问题。
4. 修复阻断问题；对缺文案/缺配图等警告明确选择继续或返回。
5. 点击“开始导出”，固定当前项目快照并展示进度。
6. 完成后显示实际输出目录、图片数量，提供“打开文件夹”“打开故事板”。

进入导出视图不强制把所有 Block 标记完成。用户可以在任务期间继续编辑，但任务始终使用开始时的同一个快照；界面提示“正在导出开始时的版本，新修改请再次导出”。

## 4. 导出前校验与一致性

复用 Phase 4 的检查规则，并加入输出路径与命名检查。

| 类型 | 问题 | 处理 |
|---|---|---|
| 警告 | Block 文案为空、素材为空、人工状态未完成 | 显示位置和数量，用户确认后可继续 |
| 阻断 | 缺失来源、文件不可读、PDF 页越界/不可解析 | 返回对应 Block/素材修复 |
| 阻断 | 非有限数/越界/空尺寸 Crop、悬空 sourceId | 保留原数据并提示重新框选或修复 |
| 阻断 | 不支持的素材渲染格式、无法取得必要尺寸 | 列明文件，不静默跳过 |
| 阻断 | 目标目录不可写、非法包名称、项目结构无效 | 就地提示并允许修改 |

没有任何 Block 时不启动导出；仅有空 Block 时允许按警告流程导出空文案/占位故事板，不伪造图片。缺配图 Block 保留编号，后续 Block 不因它缺图而改号。

流程使用两层校验：设置页预检用于反馈；任务启动后对固定快照及来源再校验。任务记录来源文件的大小、修改时间等指纹，渲染前后检测变化；检测到源文件变化则中止并提示重新导出。不要声称文件指纹检查能代替文件系统级快照。

运行中发生错误不能生成标记成功的残缺包。V1 采用全量成功才发布策略，不提供隐式跳过失败素材。

## 5. 顺序和命名

Block 按 order、素材按各自 order 排序。导出计划使用稳定 ID 映射，编号为排序后的 1-based 位置。合法输入应保证 order 唯一、连续；发现冲突时在预检中修复或阻断，不能用随机遍历顺序决定结果。

```ts
blockDigits = Math.max(3, String(blockCount).length)
assetDigits = Math.max(2, String(maxAssetsInOneBlock).length)
fileName = pad(blockPosition, blockDigits)
  + '_' + pad(assetPosition, assetDigits) + extension
```

同一素材包使用统一位数。常规项目为 `001_01.jpg`，超过 999 个 Block 或 99 张/Block 时自动扩位，按文件名词典序仍与剧情顺序一致。图片名、script 段落号、HTML 编号与映射使用同一导出计划，不能各自重新编号。

包名称禁止 Windows 非法字符、保留设备名、路径分隔符和尾随点/空格；提示用户修改，不把输入当子路径解释。同名目标已存在时默认建议新名称，不自动覆盖或合并目录。V1 不提供覆盖旧导出包，降低取消/失败时恢复复杂度。

## 6. 图片生成规则

### 6.1 输出选项

默认 JPEG、质量 92、保持原比例；提供 PNG 选项。每次导出使用统一格式，扩展名必须与真实编码一致。JPEG 遇透明区域采用白色背景；PNG 保留透明度。不做自动锐化、美化或内容判断。

普通图片默认原始尺寸输出，先统一 EXIF 方向再执行 Crop。可选“限制最长边”仅缩小、不自动放大，且在 Crop 后执行。动画格式若未提供确定的静态帧输出规则，应预检阻断并提示转换；不得无提示选取某帧。

PDF 没有与图片相同的固定像素尺寸。建议默认按旋转后的页面 200 DPI 渲染，提供 150/200/300 DPI；这属于本方案初始参数，需以现有 PDF 样本核对清晰度、耗时与内存。扫描 PDF 放大不会恢复原文件缺失的细节。

### 6.2 Crop 计算

使用 Phase 3 的统一页面方向和归一化坐标；不可对缩略图或低清预览截图导出。图片基于方向规范化后的原图，PDF 基于正确物理页的目标分辨率画布。

给定规范化页面像素尺寸 W/H：

```ts
left   = Math.floor(crop.x * W)
top    = Math.floor(crop.y * H)
right  = Math.min(W, Math.ceil((crop.x + crop.width) * W))
bottom = Math.min(H, Math.ceil((crop.y + crop.height) * H))
width  = right - left
height = bottom - top
```

输入先验证，以上边界取整用于覆盖选区和处理像素量化，不用于修复明显非法的旧 Crop。输出宽高必须大于 0。相对 Viewer 的视觉边缘误差目标不超过一个目标输出像素的取整误差。

### 6.3 渲染架构与预算

图片处理可复用 Sharp；PDF 复用现有 PDF 渲染链路，不能假设 Sharp 本身即可处理所有 PDF。将格式差异收敛到 `renderAsset(source, crop, options)`，输出受控目标文件。

耗时工作放在专用 worker 或后台任务执行器，主进程负责路径/任务权限、Renderer 展示进度。并发设上限，当前任务少量处理，不并发铺开所有页。对于超大页面计算像素预算，超限要求降低 DPI/尺寸或明确失败，不能让程序无响应。

复用渲染的键包含来源指纹、PDF 页号、方向、Crop、输出尺寸、格式、质量和渲染版本。缓存只用于加速，不成为项目唯一数据来源。

## 7. script.txt

UTF-8 编码，建议 Windows 换行 CRLF；文案正文保留原有段内换行和字符，不自动拆字幕、不合并段落、不重新润色。为确定输出，将已有换行统一到 CRLF，不裁掉正文中的有效空白。

```text
【001】

男主醒来的时候，
发现自己躺在一个完全陌生的地方。


【002】

可更加奇怪的是，房间里面竟然一个人都没有。
```

每段标题后一个空行，段落之间两个空行；空文案保留段落标题与空正文，不把“未填写文案”等界面提示写进稿件。文本中的中文、emoji 和特殊符号原样保留。

## 8. 离线 storyboard.html

页面包含项目名、导出时间、Block 数，以及按同一计划排序的文案、图片和缺项提示。图片引用如 `images/001_01.jpg`，不使用源盘绝对路径、开发服务器、CDN、远程字体或网络请求。

CSS 内联，默认无需 JavaScript。正文用 `white-space: pre-wrap` 保持换行；图片保持比例，支持桌面和窄屏布局，提供基本打印样式。大量图片可以懒加载，但打印时须验证图片完整性，不能以未加载占位交付。

项目名、文案和备注等用户输入均做 HTML 转义，不能直接插入可执行 HTML；导出的图片路径由程序生成并安全编码。包含 `<script>` 的文案应展示为文字。

空文案/空素材分别展示可读提示，文件错误在生成阶段已阻断。HTML 的检查状态只代表导出时版本，不暗示与应用实时同步。手机查看需要把整个目录及 images 一起转移，实际本地 HTML 打开能力取决于设备与浏览器；不承诺任意手机仅收到一个 HTML 就能看到全部图片。

## 9. project.json 快照与导出映射

保留开始导出时的完整项目字段，新增仅属于导出副本的 `exportManifest`，不回写工作项目：

```ts
interface ExportManifest {
  version: 1
  exportedAt: number
  projectRevision: string | number
  options: { format: 'jpeg' | 'png'; jpegQuality: number; pdfDpi: number }
  assets: Array<{
    blockId: string
    assetId: string
    sourceId: string
    file: string // 相对导出根目录，例如 images/001_01.jpg
    width: number
    height: number
  }>
}
```

实施时将实际使用的最长边、背景、渲染器版本等输出参数也记入 options。不要改变工作项目 Source 的路径，使它们指向导出后的 Crop；导出文件与原始来源含义不同。

项目快照包含原始路径、备注和收藏等信息。导出界面简要说明“项目快照包含来源路径；仅审稿时可单独分享 HTML 与 images”。这是产物内容说明，不额外阻断本地导出。

## 10. 任务状态、取消与文件安全

状态流：`idle → validating → rendering → writingDocuments → finalizing → succeeded`；执行阶段可到 `failed` 或 `cancelled`。finalizing 开始前再次检查取消标志，进入最终目录发布阶段后短暂禁用取消。

显示当前步骤、已处理/总素材数、失败来源和输出目标，不伪造剩余时间。图片完成后仍需生成文档和验证，不能提前显示 100% 成功。

在用户选择的父目录下创建唯一临时目录，例如 `.mangadesk-export-<jobId>.partial`。所有文件先写入这里，生成后校验预期文件数量、格式和相对引用，再将临时目录改名为最终目录。发布前再次检查目标名是否被其他操作占用，发生冲突则失败并提供改名重试，不覆盖。

取消时停止排队任务、安全终止/等待进行中任务，再清理本任务临时目录。失败时保留错误摘要，允许从同一设置重新导出；V1 重新执行整个任务，不做复杂断点续传。清理失败时显示遗留目录，后续仅清理有任务归属标记的临时目录。

永远不递归清理用户选择的父目录、已完成包或原始资源。每次删除临时数据前校验解析后的路径确属该任务。目录已发布后任务视为完成，不能因迟到的取消消息删除成功产物。

## 11. IPC 与模块边界

建议合约，可按现有命名风格调整：

```ts
chooseExportDirectory() // 返回受控目标 token 和展示路径
preflightExport({ projectSnapshot, options })
startExport({ projectSnapshot, options, destinationToken, packageName })
cancelExport(jobId)
onExportProgress(callback) // 返回取消订阅函数
openExportDirectory(jobId)
openExportStoryboard(jobId)
```

主进程校验 snapshot、选项和目标目录授权；只解析已登记 Source，不能提供任意路径写文件 API。文件打开使用成功任务已记录的路径，拒绝 Renderer 任意传入外部目标。保留 `contextIsolation: true`、`nodeIntegration: false`。

事件含 `jobId、stage、completed、total、error?`，切换项目或关闭导出面板后正确取消订阅，迟到事件不覆盖新任务。应用退出时如任务仍运行，提供“继续等待 / 取消导出并退出”，不要静默留下无标识半成品。

建议组件：`ExportView`、`ExportOptions`、`ExportPreflight`、`ExportProgress`；服务：`ExportPlanner`、`AssetRenderer`、`ScriptWriter`、`StoryboardWriter`、`ExportService`。命名计划、文案格式和 HTML 转义应可作为纯函数测试。

## 12. 实施顺序

1. 定义输出计划、格式选项、快照/映射及命名规则。
2. 接入 Phase 4 校验与目录选择，完成导出设置界面。
3. 完成普通图片整页/Crop 导出及像素边界测试。
4. 接入 PDF 单页/Crop 渲染和任务预算。
5. 根据同一计划生成 script.txt、HTML 和项目快照。
6. 加入临时目录发布、进度、取消、失败和冲突处理。
7. 实现完成后的目录/故事板打开。
8. 用真实图片和 PDF 做整包验收，再在剪映中人工检查按名称导入顺序。

## 13. 测试与完成定义

| 测试 | 验收标准 |
|---|---|
| 典型 3 段多素材 | 图片名、TXT、HTML、映射四处顺序一致 |
| 超过 999 段/99 素材 | 统一扩位，按名称排序不乱序 |
| 同源多次引用 | 每次引用有独立计划项/文件，与各 Block 一一对应 |
| Crop 四角/反向框选结果 | 导出内容与 Viewer 相符，无黑边、错页或越界 |
| EXIF/PDF 旋转 | 方向与框选坐标一致 |
| 中文路径、emoji、HTML 字符 | 文件可读，TXT 不乱码，HTML 正确转义且不执行脚本 |
| 缺文案/缺图 | 警告确认后保留段落编号和占位 |
| 源文件丢失/任务中变化 | 不发布成功包，指出具体来源并能重试 |
| 目标冲突/磁盘满/无权限 | 旧文件不变，错误可理解，半成品有明确归属 |
| 取消/退出 | 任务停止且不误删来源或旧导出，成功发布后不被迟到取消删除 |
| 导出期间继续编辑 | 所有产物属于开始时同一版本，无前后混合 |
| 移动素材包离线查看 | 整个目录移动后 HTML 正常显示，不依赖网络或原目录 |
| 快照追溯 | 每个导出文件可回查 blockId/assetId/sourceId |
| 剪映人工检查 | 选定测试版本按文件名排序后与故事板一致；不宣称自动排列时间轴 |

性能测试记录机器、PDF DPI、源尺寸、素材数、峰值内存与总耗时。初始样本建议 200 个 Block、1000 次素材引用；界面始终可响应取消，内存随受限并发/缓存而有界，不随全部页画布无限增长。

Phase 5 完成需满足：一次导出获得全部四类产物，整页/Crop/PDF 清晰且顺序正确，离线 HTML 可移动查看；失败和取消不会污染旧包或原文件；保存的工作项目不因导出而改变。下一阶段可复用 `AssetRenderer` 和导出计划设计，实现剪映辅助窗口与按需原生文件拖出。
